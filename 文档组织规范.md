# 文档组织规范

本规范是**通用**写作约定，适用于博客所有板块（`ai/ frontend/ interview/ node/`）。它是一份写给 AI（以及协作者）的指引：每次新建或修改博客文档前，先按这里的约定产出，再以仓库内配套校验脚本兜底。约定已在 `node/` 全量落地，可作为现成实例参考。

---

## 1. 板块、模块与配套代码

博客基于 VitePress（`base: '/blog/'`），按内容领域分板块，板块下再分「模块」。

- 编号规则：**所有目录与文档文件名都不带数字前缀**（含章节文档）；真实阅读顺序以**侧边栏**为准，篇间引用写「见第 X 篇」（X 按侧边栏顺序）。
- 配套代码放在 `<板块>/<模块>/code/<项目名>/`，被 `srcExclude: ['**/code/**']` 排除出页面集合，不进导航、不进构建产物。

实例：

```
node/
  运行环境/                模块目录（不带编号）
    code/                    配套可运行代码
      node-basics/           code 下的项目
    Node.js 是什么.md      章节文档（文件名不带编号）
  总结.md                     模块元页面（见第 5 节）
  面试题.md
```

### 测量型模块：配套代码做成「实验台」

当文章里的结论必须是**可复现的真实数字**（性能、监控、并发、内存这类模块）时，配套代码不写成"演示页"，而写成一个**实验台**：每个现象对应一个能跑出数字的场景脚本，文章里的实测输出块全部来自它的 stdout。本仓实例：`frontend/进阶/性能优化/code/perf-lab`、`frontend/进阶/监控与稳定性/code/monitor-lab`、`frontend/进阶/网络与协议进阶/code/net-lab`。

```
<模块>/code/<lab>/
  harness.*       脚手架：对齐表格、起服务、起无头浏览器、等结果
  scenarios/*     一个现象一个脚本，各自能单跑（runAsMain 判断主入口，或用 cli.mjs 分发）
  pages/ 或 site/ 浏览器侧的被测页面
  package.json    每个场景一条 script，名字与文章里的「运行：npm run xxx」逐字一致
```

几条固定做法：

- **零依赖优先**：只用语言内置能力起服务与浏览器（`PORT=0` 让系统分配端口，再从进程输出里回读真实端口）。
- **让差异可观测**：本机网络下什么都一样快，需要差异时在**服务端**按限速切片发送，把差别放大出来。
- **输出要可复现**：随机数据用固定种子，时间戳对齐到整秒，否则写进文章的数字每次都不一样。
- **依赖注入**：浏览器 API 从 `options.env || globalThis` 取，同一份实现既能进浏览器也能在 Node 里跑单测。
- **口径差异要显式打印**：同一现象两套口径会给出不同的数（窗口 vs 秒桶、`cls` vs `clsRaw`），输出里就要把差异打出来并解释，否则读者会当成 bug。

---

## 2. 单篇文档体例

一篇标准文档的固定骨架：

```markdown
# 篇标题

## 小节标题写成问题或判断句
正文……（可从脚本摘录代码，见第 3 节）

## 小结
（知识树，见第 4 节）

## 配套代码
（表格，见第 3 节）

## 参考
- [模块总结](./总结.md)
- [模块面试题](./面试题.md)
```

约定要点（用例子说明，不逐一列禁项）：

- **小节标题**写成问题或判断句：`require 与 import 的本质区别是什么？`
- **篇间引用**写「见第 X 篇」，不硬编码文件路径。
- **结尾三段顺序固定**：小结 → 配套代码 → 参考；`## 参考` 前两行固定为本模块「总结 / 面试题」入口。
- **ASCII 图**用裸 ```` ``` ```` 围栏，不要写成 ```` ```text ````（VitePress 会刷告警）。

---

## 3. 代码片段与配套代码表

正文里的代码块**必须逐字来自标注的脚本**——缩进、注释、变量名、箭头语法（`x =>` 与 `(x) =>`）都照抄，不允许手抄改写。每个代码块上方一行标注来源。

来自可运行脚本：

```markdown
> 摘自 `node/运行环境/code/node-basics/src/02-module.js`（运行：`npm run module`）

```js
const path = require('path')
// …
console.log(path.resolve('a', 'b'))
```
```

无对应脚本的纯 API 速查，标注为示意片段：

```markdown
> 示意片段（无配套脚本）
```

省略处用单独一行 `// …` 表示，省略不能破坏代码连续性（按省略行切段后，每段都要能在脚本原文里找到）。

`## 配套代码` 表——路径写纯文本、不链接，并列出覆盖的小节：

| 脚本 | npm script | 对应小节 |
| --- | --- | --- |
| `code/node-basics/src/02-module.js` | `npm run module` | 模块解析规则 |

标注的脚本必须出现在本篇配套代码表第一列；`npm script` 名、lab `README.md` 的表、`## 配套代码` 表三处保持一致。

受检的语言不止 JS：`javascript / js / typescript / ts / css / scss / html / jsx / tsx / vue / json` 都要写出处或标「示意片段」——写 `html` 围栏展示页面结构与属性时同样如此。**不标语言的裸围栏不受检**，所以实测输出与 ASCII 图用裸围栏；不要在围栏上加 `text` 这类语言名，多一个名字就多一条构建告警。

**正文（行内代码、表格、普通段落）里要写双花括号时，用 `<code v-pre>` 而不是反引号**——VitePress 只给围栏代码块加 `v-pre`，行内代码里的双花括号会被 Vue 当插值解析，构建直接报 `Error parsing JavaScript expression`；围栏代码块（含不标语言的实测输出块）不受影响，照常写。

也不要用 `&#123;&#123;` 这类 HTML 实体绕：markdown-it 会把 `&` 再转义一次，页面上原样显示出 `&#123;&#123;` 这几个字符，既不报错也不对。实体只在这份规范自身的示例里出现。

````markdown
正文里写 <code v-pre>{{ item.name }}</code>，不要写成反引号包起来的样子。

```wxml
<view wx:for="{{ items }}">{{ item.name }}</view>
```
````

---

## 4. 小结：知识树

`## 小结` 放在 `## 配套代码` 之前，是从本文提炼的知识骨架。

```markdown
## 小结
- **概念域一**：一句话结论
- **概念域二**
  - **知识点**：一句话结论
  - **知识点**：一句话结论
- **概念域三**（要点多时在中间加一层子域分组）
  - **子域 A**
    - **知识点**：一句话结论
  - **子域 B**
    - **知识点**：一句话结论
```

约定：

- 第一层是有信息量的**概念域**，且必须是干净标签——不要把结论塞进标题（如写「I/O 密集型」，不要写「为什么是 Node：耗时绝大部分是"等"出来的」）。
- 概念域直接挂一句话结论，要点多时可在中间加一层**子域**分组；知识点层只写一句话结论，**不写解释**，展开留给正文——小结只做"框架锚点"。
- 同一结论只说一次：**不得重复论证**——分散在多处的同义表述压成唯一一条；语义重叠的域（如"代价清单"与"不适用边界"）合并为一。属于某个系统的细节（如 CJS 的缓存、循环依赖、`exports` 陷阱）就收进该系统自己的概念域，不再单列。
- 形态：最多三级、每级缩进 2 空格、同一层 ≤ 8 条；概念域数量以"能建框架"为度，宁少勿滥。
- 少数导读 / 收束篇可不写小结。

模块级「总结.md」的 `## 分篇知识点` 由各篇小结**反向聚合**生成：只改篇小结，由脚本重建模块总结（幂等），不要手工分别维护两级。

---

## 5. 模块元页面：总结.md 与 面试题.md

每个模块配套两份元页面（在 `node/` 内按主题合并为 6 组：01 · 03 · 04 · 05 跨 06 · 07 跨 08 · 09 跨 10,11）。

**总结.md** 结构示例（只有两节，不加速查表 / 见 X 想 Y / 易错点汇总 / 模块接口 / 自测入口 / 参考等附加段）：

```markdown
# 模块总结 · 运行环境
## 知识主线           用 ASCII 链讲清篇间依赖
## 分篇知识点         按篇聚合的知识树（见第 4 节）
```

**面试题.md** 按四段组织，每题标注出处篇号：

```markdown
# 模块面试题 · 运行环境
## 一、基础概念
### Q1: ……（第 01 篇）
结论 / 原理 / 边界 / 追问
## 二、机制与原理
## 三、排障与选型
## 四、场景设计题
```

---

## 6. 改动后的校验

改完文档按序跑一组校验：链接检查 → 侧边栏覆盖 → 代码同步 → 构建。其中「代码同步」由仓库内配套脚本兜底（可按模块 `--module` 缩窄范围），核心判据只有一条：**代码块标注存在 + 摘录逐字可溯源 + 标注脚本在本文配套代码表中**。

VitePress 构建会被宿主的安全删除保护拦在「清空 `.vitepress/.temp`」这一步（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`，阈值 50）。绕法是让构建进程不加载该保护包装——清空 `NODE_OPTIONS` 再启动：

```bash
NODE_OPTIONS= node node_modules/vitepress/bin/vitepress.js build .
```

注意 `--outDir <目录>` 这个参数 VitePress **不识别**（传了也不生效），产物始终写在 `.vitepress/dist`，判断构建是否成功要看产物（目标板块 html 数与 `hashmap.json`），不能只看日志里的 `✓ rendering pages`。
