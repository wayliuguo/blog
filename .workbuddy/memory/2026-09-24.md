# 2026-09-24 工作日志

## 性能优化 perf-lab 第五轮：删除 harness、改为纯手动对照
- **动因**：用户要求"不要这么复杂，通过手动打开优化前后的来对比就可以了"。上一轮已加的 4 个 `?act=` 页面内实验本就不走 harness，本轮把整个 harness 链路也砍掉。
- **删除**：`code/perf-lab/harness/`（lab.mjs/index.mjs/server.mjs/chrome.mjs/table.mjs，用 python `shutil.rmtree` + `PYTHONPATH=` 绕过 safe-delete shim）；`package.json` 删 `lab`/`lab:list` 两条 npm script，只留 `dev:before/dev:after/build:before/build:after/build/preview:before/preview:after`。
- **配套代码改造**：`DetailView.vue` 不再依赖 harness `/asset?name=..&kb=..&kbps=..` 动态端点，改为 `shared/public/lab-assets/` 下提交的静态 SVG（`hero.svg` 800×300 + `shot0..11.svg` 320×240，由 `C:\Users\10855\.workbuddy\tmp\gen_lab_assets.py` 生成，Vite `publicDir` 自动拷到 dist）。`lab.js` 删 `/report` fetch 分支，只 `console.info('[perf-lab] 本次实测', payload)`。
- **关键坑（已记入 MEMORY.md）**：`?act=` / `?key=` 等开关**必须写在 `#` 之前**（读 `location.search`），`#/list?act=x` 这种 fragment 形式会被 `drive()` 静默忽略。README 实验表已统一改成 `index.html?act=nav#/list` 形态。
- **文档全改**：`README.md`（重写，12 实验一表：层/`?act=`/对照变量/打开哪个 URL/看哪里）、`性能优化实战.md`（删 lab:list/npm run lab/CHROME_PATH/puppeteer/800kbps，二章只列 dev/build/preview + DevTools Slow 3G；三章改为"8 基础 case + 4 页面内对照"；口径章重写：INP 近似、DevTools 限速、不取中位数、CLS 4 位小数）、`性能优化体系与指标.md`（删 `npm run lab` 说明、`800kbps`/`无头` 引用，更新 DetailView/lab.js 代码块）、`总结.md`（2.2 重写）、`面试题.md`（800kbps→本地静态 SVG 等）。
- **校验**：`check-code-sync` 16/16、`check-links` 0 问题、`check-sidebar-coverage` 全可达、`vitepress build` exit 0（146s，后台跑 + 重定向日志，确认 dist 新字符串 mtime）。
- **教训（重要）**：VitePress 文档站**不重建** perf-lab 这两个 vite 工程（只引用源码、不构建）。本轮加 `lab-assets/` 后首查发现 `dist-after/lab-assets/` 缺失（preview 会 404），已手动 `npm run build`（perf-lab 根，NODE_OPTIONS= 绕 shim，EXIT=0，27s）重出 `dist-before/`/`dist-after/`，两目录均已含 13 个 SVG。
- **清理**：两处 `apps/*/vite.config.mjs` 里残留的 "harness 与产物"/"harness 的静态服务器" 注释已改为无 harness 表述。
- 实测体积基准（after 版）：`optimized 56.80 kB`(gzip 23.12)、`DetailView 3.90`、`ListView 5.45`、`ReportView 8.41`、`index.html 2.82`(gzip 1.27)。

## 性能优化体系与指标：二~四章加「本章手段总览」
- **动因**：用户指出第二~四章「每一层级涉及的手段应先汇总再展开」，原文档直接进 定位/优化，缺体系化陈述。
- **改动**：在 二(加载层)/三(运行层)/四(视觉层) 每章开头（原有 LCP四段/INP拆三段/两条判定规则 之后）各插入一张「本章手段总览」表：列 = 子层 | 服务该指标的哪一段/哪类位移 | 手段清单（逗号分隔的 手段 速查）。并注明「跨指标归属见第五章 5.2 总矩阵；以下逐层/逐类展开」，与既有的 5.2 跨指标矩阵互补而不重复。
- **校验**：check-links 0 问题 / check-sidebar-coverage 全可达；我改的 性能优化 模块 check-code-sync 0 问题（其余 40 处失败在 webpack/WebAssembly/平台 等无关模块，属历史遗留、本次未动）。vitepress build EXIT 0（沙箱拦截 .vitepress/dist 写，需 dangerouslyDisableSandbox 重跑），产物 HTML 已含 3 张总览表（grep 验证）。

---

# 2026-09-24 工作日志

## 性能优化篇 体例第三轮：去「总览」+ §1.4 拆分（已定方案，待执行）

### 用户三个决议
1. **§1.4 走 A1**：拆成独立章 `## 二、口径与工具`，原二~五顺延为三~六。
2. **结构修正**：子结论用有序/无序列表，**其解析与佐证缩进一格**挂在条目下（不要「总纲/结论/观点或佐证」五段，也不要「**论点**：/**佐证**：」显式加粗标签另起段）。
3. **ASCII 图不是必须**（按需，不再每层强配）。

### 新体例（三层钻取，去掉中间标题）
篇 `#` → 章 `##` → 子层 `###` → 手段 `####`。
- 章/子层：**标题即结论**，标题下**直接放结构化表达**，**不再有「总览」标题**。
- 结构化表达选形：并列对比→表格；并列条目/先后→有序或无序列表；结构流程说不清→ASCII（按需）。
- 手段 `####`：正文 = 子结论列表（每条下缩进解析 + 佐证）；证据（代码块/读数表）逐字保留。
- 依据（外部最佳实践）：BLUF/倒金字塔、「不要给结论贴可见标签」、「只读标题能串出故事」、渐进式披露、列表表格替代密段、claim→evidence→implication。本仓 `node/` 本来就是「标题→结论→表/列表→`> 摘自` 代码」，无「总览」层。

### A1 章号映射（执行时按高→低序替换，避免串号）
| 原 | 新 |
|---|---|
| 一、指标体系 1.1~1.3 | 不变 |
| （§1.4 口径与工具） | **二、口径与工具**（2.1 口径 / 2.2 工具） |
| 二、加载层 2.1~2.4 | 三 3.1~3.4 |
| 三、运行层 3.1~3.4 | 四 4.1~4.4 |
| 四、视觉层 4.1~4.4 | 五 5.1~5.4 |
| 五、收口 5.1~5.3 | 六 6.1~6.3 |

### 连带改动清单（已量化）
- 体系篇内部：约 50 行含章节号引用（`见 2.3` / `→ 2.4` / `3.1 节` / `第五章 5.2` / `见 1.4`）需改号。
- **实战篇**：8 处 —— 第 228 行「3.3 框架层、4.2 字体、4.3 动态内容、4.4 动画」+ 第 237~240 行表格首列「3.3 重渲染 / 4.3 动态内容 / 4.2 字体 / 4.4 动画」。
- **总结.md**：第 117 行同样那 4 个引用。另发现**既有两级不一致**：大綱 2.3 写「八个 case 与四个手动对照实验」，正文 `### 2.3` 写「十二个实验（8 个基础 case + 4 个页面内对照）」——顺手统一。
- **面试题.md**：题目标签用的是**总结.md 大纲号**（Q8（1.4）= 加载层），与体系篇章号解耦，**不用改**。
- 关键发现：**总结.md 的知识域分层本来就把「口径与工具」独立成 1.3**，A1 让篇的章结构与之对齐。

### 文件
- 待改：`frontend/进阶/性能优化/性能优化体系与指标.md`、`.../性能优化实战.md`、`.../总结.md`、`文档组织规范/reference/单篇体例.md`（`## 长节用「总览 → 手段」钻取结构` 一节要重写为「标题即结论 + 列表缩进佐证」）。

---

## 第三轮：执行结果（已完成）

### 体系篇 `性能优化体系与指标.md`（1071 行）
- **§1.4 拆出独立成章**：`### 1.4 口径与工具` → `## 二、口径与工具`（`### 2.1 口径：数据从哪来、看哪个数、怎么读评级` + `### 2.2 工具：按「替你做掉什么决定」选，按「粗 → 细」用`）。章内再分 6 个工具小节 `#### Network 面板 / 瀑布图 | Performance 面板 | Coverage 面板 | Memory 面板 | Lighthouse | Performance API / 实验台`。
- **章号整体顺延**：原二/三/四/五 → 三/四/五/六；小节 `2.x→3.x`、`3.x→4.x`、`4.x→5.x`、`5.x→6.x`。**单遍映射替换**（不做链式），并跳过 4 处紧邻 `s/ms/MB/KB` 的数值（是数据不是节号）。
- **删除全部 `### 总览` / `#### 总览` 标题**（24 个），**删除「**论点**：/**佐证**：」显式标签**，标签行转成列表项；注意事项并入列表，不单开节。
- **保留全部代码块与示例读数表逐字**（29 对围栏不变，check-code-sync 16/16 通过）。

### 连带改动
- **实战篇** 8 处引用改号：`3.3 框架层、4.2 字体、4.3 动态内容、4.4 动画` → `4.3 / 5.2 / 5.3 / 5.4`；配套表首列 4 处同改。
- **总结.md** 5 处：同一组引用改号；顺手**修既有两级不一致**——大纲 2.3「八个 case 与四个手动对照实验」→「十二个实验（8 个基础 case + 4 个页面内对照）」（与正文 `### 2.3` 对齐）。
- **面试题.md 不动**：其 `Q8（1.4）` 用的是 总结.md 大纲号，与体系篇章号解耦。
- **文档组织规范**：`reference/单篇体例.md` 的 `## 长节用「总览 → 手段」钻取结构` **整节重写**为 `## 长节：标题就是结论，证据跟在结论下`（骨架示例 + 5 条要点）；`readme.md` 摘要补一句长章三层钻取。

### 校验（全绿）
- `check-code-sync --board frontend --module 进阶/性能优化`：2 篇 / 16 代码块 / 缺失 0 / 对不上 0 / 表里没有 0。
- `check-links --board frontend`：183 篇 / 676 链接 / 0 问题。
- `check-sidebar-coverage --board frontend`：183 篇 / 183 侧栏链接 / 0 未覆盖。
- `vitepress build`：exit 0（203.28s）。grep 产物确认：`二、口径与工具`/`2.1 口径：数据从哪来`/`2.2 工具：按`/`三、加载层`/`四、运行层`/`五、视觉层`/`六、收口`/各工具 `####` **全部 OK**；`### 总览`/`总纲`/`观点或佐证`/`事项：具体手段`/`1.4 口径与工具` **全部 absent**。`单篇体例.html` 含 `标题就是结论`。

### 复用要点
- 章号重编用**单遍映射**（`re.sub` + 映射函数）而非链式 `str.replace`，避免 2.x→3.x→4.x 串号；并**排除「数字+单位」的误伤**（先扫一遍看哪些是数据）。
- 删标题 / 删标签这类批量整形，用 Python 逐行状态机（跟踪 `in_fence` 不碰围栏内），比正则全局替换安全。
- MEMORY.md 已从 8819 压到 7505 字符（合并两段重复的 perf-lab 描述、删历史篇清单的细节）。
