# 2026-09-20 工作日志

## 07:25 L2 收官状态复核（只核查，无新产出）

- 复跑四件套全绿：sidebar 覆盖 frontend 198:198 / node 78:78 / ai 3:3，links 1109 条 0 问题，code-sync frontend 1251 块（对齐 568 + 示意 683）0 问题、node 417 块 0 问题，`vitepress build` 72.58s exit0 无告警
- 工作区干净，`master` 与 `origin/master` ahead/behind 均为 0；HEAD = 36f9ef2
- 进阶 19 个模块篇数：11/9/9/9/7×6/6×6/5×3，共 131 篇，另有板块级独立入口 `面试方法论.md`
- **遗留未决**：`frontend/2026-09-18-frontend进阶体系化改造方案.md` 在 47148cd 被并行会话删除，可从 `47148cd^` 取回；`.workbuddy/plans/` 下另有 6 份 09-17/09-18 的方案文档在册
- **用户决定**：① 方案文档不恢复 ② 重审进阶模块知识深度：构建工具（Vite/Rollup）要深入、插件案例要贴真实场景、性能优化要加一个 Vue SPA 闭环 ③ Vue 依赖**允许 npm install**（perf-lab 不再是零依赖）④ 范围＝点名两项 + Top5 薄弱模块全补

## 07:40 深度审计（三路并行 Explore）

- 构建体系：Vite 8.4k、Rollup 9.5k 是 8 篇里最短的两篇，却最重要（Webpack 篇 12.3k 反而最厚）——深度与重要性倒挂。插件案例玩具度黑名单：plugin-lab 三个（虚拟 BUILD_INFO 三实现）、rollup `custom-plugin.cjs`（虚拟模块+加注释）、vite `counter-plugin.mjs`（计数）
- 性能优化：7 篇合计 8.4k，实验全是单页 HTML，无 SPA；零覆盖 modulepreload / immutable / SW / 第三方脚本治理 / INP
- 其余 17 个模块 Top5 薄弱：交付与质量（7 篇 4.4 万字但**零实验台**）→ 工程素养（1.67 万，最薄）→ 研发平台 → 浏览器底层与 V8（1.98 万）→ 可视化（SVG/WebGL 无配套代码）
- 共性缺口：普遍缺「反面案例」与「线上故障排查」；版本差异几乎为零

## 08:0x P1 构建体系·Rollup 深入（6 个新脚本 + 正文 9.5k→15k+）

- 新增 `rollup-lab/`：`hooks.cjs`（钩子全景，build 12 钩子里前 7 个只跑 1 次、第二次 generate 后 build +0 / output +1）、`context.cjs`（PluginContext：getModuleIds/getModuleInfo/resolve/emitFile/warn+error，3 模块 3 边 1 环，394B manifest，failOnCycle 一开就中断）、`side-effects.cjs`、`external.cjs`、`size-gate.cjs`（真实插件：gzip 门禁 + manifest）、`cjs.cjs`（手写 20 行 commonjs 替身）
- 实测口径：external 前后 **10170 → 23 字符（440 倍）**；UMD+globals 621 字符且含 `global.React`；PURE 注解：带注解 `make('pure')` 被删、紧邻不带的保留（404 字符）；`moduleSideEffects:false` 时 **lib.js 顶层 console.log 仍保留**（它只让"未引用模块整块删"）
- 踩坑：① PURE 对比如 `make()` 太简单，Rollup 自己能证纯 → 必须给工厂函数加 `window.__lastTag = tag` 才有对照 ② external 差异起初拉不开（未用导出被摇掉）→ 改成 `export * from 'react'` 让 200 个导出一个都摇不掉 ③ `closeBundle` 只在 `bundle.close()` 时触发，只 generate 不等它
- 正文替换：虚拟模块玩具案例 → 产物体积门禁（真实场景）；新增钩子全景 / PluginContext / external 四开关 / CJS 互操作 / 与 Vite 的透传关系；总结 + 面试题（Q28-Q32）同步

## 08:2x P2 构建体系·Vite 深入（2 个新脚本 + 正文 8.4k→14k+）

- 新增 `vite-lab/assets.cjs`：assetsInlineLimit（0.6KB 内联 / 10.8KB 发文件，阈值 512 后前者也变文件）、build.target（es2015 下 `??` `?.` 都被改写，4863→5135 字符）、glob（懒加载 4 chunk vs eager 1 chunk）
- 新增 `vite-lab/plugins.cjs`：enforce 顺序实测 `pre → normal → post`；真实插件 htmlGuard（transformIndexHtml order:post 注入 modulepreload + CSP nonce）；**不查重会注入 2 条**（Vite 默认已注入 1 条）——这是本次最有价值的坑
- 踩坑：① 顺序探针被模块数放大 → 按 id 分组只取 main.js ② 动态 import 走 `dynamicImports` 不是 `imports` → 改静态 import + manualChunks 切 chunk 才有可注入的依赖 ③ 正文写 `?? \"default\"` 多了反斜杠 → code-sync 报「对不上」，**已转义与文件内容必须逐字一致**，用 Python 修
- 生成的样本目录（src-ext/src-assets/src-plugins）已加进 build-lab/.gitignore，与 analyze-lab、bench 的惯例一致
- 校验：code-sync 构建体系 63 块全对齐 · links 1112 条 0 问题 · sidebar 198:198 · build 68.06s exit0 无告警

## P3 构建插件开发篇换真实场景（提交 b8ccf67，已推）

- 主案例从「虚拟模块」换成两个生产场景：① 组件库按需引入（unplugin 跑 Rollup/Vite/webpack 三处，假组件库 ui-lib 带顶层注册副作用，实测未引用组件被剔除）② 禁用 API 门禁（@babel/traverse 扫 localStorage/console，warn 构建通过只告警、error 中断）；虚拟模块降为「三·一」对照小节
- 新坑 7：`.mjs` 里 require CJS 包要 `createRequire(import.meta.url)('@babel/traverse').default`——直接 import 拿到 `traverse is not a function`
- 踩坑：门禁 `this.error` 先抛导致后续 sink.push 不执行 → 命中 0；把 push 提到报错前
- 「表里没有」类错误：正文引用了 vite-lab/counter-plugin.mjs 但配套代码表没登记 → 补一行

## P4 性能优化·Vue SPA 闭环实验（新第 8 篇）

- 新增 `perf-lab/pages/spa/`：真 Vue 3 SPA（hash 路由 + 3 路由模块 + 2000 订单）。两版外壳 = 两套决策：`index.html`+`app.js`（CSS 外链阻塞 / 空壳 / 路由静态全量）vs `optimized.html`+`app-opt.js`（关键 CSS 内联 / 骨架屏 / `() => import()`）；共用 `boot.js`（hash 路由 + 懒加载 + 空闲预取 + `?act=` 实验驱动）、`options.js`（开关：`opt=1` 全开，单项可覆盖）、`store.js`、`routes/`、`routes-all.js`、`sw.js`
- **服务端**：`/vendor/vue.js` → `node_modules/vue/dist/vue.esm-browser.prod.js`（浏览器直接跑 ESM，无构建步骤）；`startServer(options)` 新增 `port` / `spaCache` / `spaLatency`；`openChrome(url, flags, profileDir)` 新增第三个参数
- 四场景实测（3 轮中位数）：首屏 FCP 508→224 / 首屏有内容 247→94ms / 关键路径 JS 10→7 个 / DOM 10023→99；切路由 69（全量）/245（分割）/80ms（分割+预取，150ms RTT）；排序交互到下一帧 134→31→28ms；滚动 3985ms（最长任务 3035ms）→1007ms（0 个）；二次访问 289→278（无缓存）/266→122（HTTP 强缓存，0B）/276→205ms（SW，0B）
- **三个反直觉结论**：① 骨架屏让 LCP 落在「加载中…」上，指标变绿但用户没看到内容 → 必须配「内容就绪」自定义指标 ② 路由分割几乎不省体积（152.2→149.1KB，Vue 运行时占 131.5KB），省的是关键路径请求数 ③ **SW 比 HTTP 强缓存慢 ~80ms**（冷启动线程 + 每请求过 fetch），价值在离线与按资源分策略，不是「更快的缓存」
- **实验坑（重要）**：① HTTP 缓存按 origin 存，端口随机（listen 0）会导致「缓存没生效」的假象 → 缓存场景必须固定端口 5192 ② 本机 localhost 秒下，加载类对比必须人为加延迟（spaLatency）③ 无头环境无真实输入，脚本 click() 不产生 event 条目，INP 只能用「派发→第二个 rAF」近似 ④ 强制同步布局要「写完就读」才成立，只读不写测不出差异
- 新篇 `Vue SPA 性能实战.md`（第 8 篇）：五段闭环（加载/切路由/运行时/缓存/闭环）+ ROI 排序；侧边栏登记、总结.md 补 08、速查篇「下一篇」改指新篇
- **老坑复现两次**：① 同一文件并行 Edit 互相覆盖（server.mjs 的 spaLatency 声明被冲掉、spa.mjs 的一处文案被冲掉）② `python -c` 里写反引号被 shell 当命令替换吃掉（总结.md 08 段丢了 `` `media="print"` ``）

## P5 交付与质量·补 cd-lab 实验台（此前零实验台，只有 3 个静态演示页）

- 新建 `code/cd-lab/`：`cli.mjs` + `harness/table.mjs`（复制 perf-lab 那套中英混排表格）+ 三个场景 + `fixtures/`（运行时生成，已 gitignore）
- `lint.mjs`（**实测**，真 ESLint 8，从仓库根 node_modules 解析，可用 `ESLINT_DIR` 覆盖）：1000 文件样品工程，全量 3035ms/3000 问题 vs 只查改动 12 个 36ms/36 问题；规模扫描 100/300/1000 → 单文件恒 3ms，**总耗时对文件数线性**
  - 盲区实测：改了 a.js 导出名，只查 a / 只查 b / 全量查，ESLint 全报 0（它是单文件分析，不解析 import 指向的文件）
- `coverage.mjs`（**模型**）：卡**全项目**覆盖率是二值的（70% 不拦、80% 全拦，因为改动被 20 万行存量稀释）；卡**改动文件**才有分辨力（阈值 60→90%，误杀 22.3%→88.9%，召回都 ~100%）；测试失败召回 66.3%/误杀 2.0%/精确率 85.7%
- `release.mjs`（**模型**）：跑满再判 → 100% 用户受影响（第 4 批发现）；每批都判 → 第 1 批、5%，代价 0.2% 误回滚；**最小样本量 ≥3000 时第一批 2500 样本「不够」→ 受影响 5%→20%，早停形同虚设**
- 三篇正文各加一节实测/模型结论（门禁篇 §五、质量体系篇 §2.1、发布篇 §四.3），篇小结与总结.md 同步补数字
- **模型类实验必须在正文与脚本顶部明示「这是模型不是实测」**，并把假设写成可调常数
- 已有 `code/site/`（3 个静态演示页 + server.js 端口 5183）保留不动

## P6 工程素养·真实 P0 事故案例切段（提交推送）

- 在 `practice-lab/incident.cjs` 加场景四：复盘行动项 owner/截止/验收三缺一即判不合规，实测 5 条只闭环 1 条（20%）、1 条逾期 8 天、另三条分别缺 owner/缺截止/缺验收
- 线上事故复盘篇 §二 改写：把一段模糊的「止血优先」拆成可量化的三段 —— ① MTTD/MTTR 切段（实测 35+15min，补监控后 MTTD 5min 而 MTTR 不变，先分清慢在哪段）② 止血三件套（回滚/降级/限流）③ 复盘结论必须是「关掉的行动项」而非文档
- 总结.md 03 节同步；practice-lab 全探针 `node run.cjs` 仍全绿

## P7 研发平台·指标被玩坏 + 季度曲线（提交推送）

- `platform-lab/metrics.cjs` 原 4 场景全保留；修正场景（30 天 20 次变更、最后一周含 1 次失败）→ 完整窗口 2/20=10%，掐掉最后一周 1/23≈4.3%（指标窗口裁剪的造假手法）
- 研发效能度量篇 §三 重写：DORA 四指标分档 + 反模式（LOC 考核让 LOC −60% 但前置 48h→12h、故障 3→0；拆 PR 让同样 200 行改动部署次数翻 5 倍）+ 三条原则（度量团队不度量个人 / 多指标交叉 / 只找瓶颈不排名）
- 总结.md 03 节同步数字

## P8 浏览器底层与 V8·补多进程实测 + 泄漏场景 + 版本演进（构建中，待提交）

- **多进程探针 `v8-lab/ipc.cjs`（新建，零依赖，fork 子进程）**：① 崩溃隔离（一进程炸另正常）② 内存不共享（结构化克隆副本，子改父不变）③ 序列化代价（1 万条≈20ms、100 万条≈1.3s，比同进程遍历慢 18→200+ 倍）④ 每进程固定开销≈48MB
- 多进程篇新增「六、进程边界实测」章 + 配套代码表登记 ipc.cjs；run.cjs 加探针四
- **泄漏场景补两类可实测**：`gc.cjs` 原只有「可回收+全局缓存」两场景，新增「定时器闭包」「监听器闭包」两类（各实测 GC 后占 43MB，clearInterval/off 后回基线）
- **关键坑**：本机 `global.gc()` 对「模块顶层 const 大数组」永远收不掉（被模块词法作用域长期持有，快照不显示该边）→ 泄漏场景必须包在函数里（timerLeak/listenerLeak 函数局部），函数返回后随作用域结束回收
- V8 篇 §三 加「版本演进」小节：Ignition→Sparkplug(2021/Chrome91/V8 8.6)→Maglev(2023/Chrome117/V8 11.4)→TurboFan 四档管线；明确 `opt.cjs` 的 `%GetOptimizationStatus` 状态位掩码随 V8 版本变化（本机 Node22=V8 12.x）
- 内存篇 §四 补定时器/监听器泄漏实测代码块（逐字对齐 gc.cjs scenario3/4，含驱动行）；总结.md 01 节登记多进程探针
- 校验：code-sync 浏览器底层与 V8 14 块全对齐（修复装饰横线字符数不一致、代码块漏驱动行两处「对不上」）；links 1115 条 0 问题；sidebar 199:199
