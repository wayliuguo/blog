# 2026-09-23 工作日志

## 性能优化模块：体系篇 / 实战篇 / 文档组织规范 收口

### 体系篇（性能优化体系与指标.md）
- **二~四章（12 个子层）全部用「论点-佐证」结构化**：`### 子层` + `#### 总纲 / 结论 / 观点或佐证 / 注意事项 / 事项`。所有代码块、示例读数表、总览表**逐字保留**（check-code-sync 仍 0 问题）。
- **§1.4 口径与工具**：从「散段落 + 啰嗦」重构成同样的 `####` 结构（总纲定调 -> 结论先给答案 -> 佐证只陈列表格/代码/现象对查表 -> 注意事项兜底 4 个坑 -> 事项给工具使用顺序）。删掉 Lighthouse/分工里重复的铺垫句，表格与 `lab.js` 的 CLS 双口径代码块保留。

### 实战篇（性能优化实战.md）
- 删篇首陈述（两段引导）。
- 删「为什么拆成两个工程」「为什么不复制源码成两份」两段理由（用户明确不关心）。
- 修复一处因替换引入的「两个工程、一套源码」重复句。

### 文档组织规范
- `reference/单篇体例.md`：新增 `## 长节用「论点-佐证」结构`，把 `#### 总纲/结论/观点或佐证/注意事项/事项` 定为长节标准写法，指向体系篇二~四为范例。
- `reference/板块与配套代码.md`：实验台入口形态「二选一」改「三选一」，新增 **③ 纯手动对照**（perf-lab 现形态：两个独立 vite 工程 + 共享源码、无 harness/CLI、`?act=` 页面内 console 读数、README 给实验<->篇目对照表）。旧文档仍写「harness/lab.mjs」属过期，已更正。

### 校验
- `check-code-sync --board frontend --module 进阶/性能优化`：2 篇 / 16 代码块 / 全通过。
- `check-links --board frontend`：183 篇 / 676 链接 / 0 问题。
- `check-sidebar-coverage --board frontend`：183 篇 / 183 侧栏链接 / 0 未覆盖。
- VitePress build 跑通（见 `C:////Users////10855////.workbuddy////tmp////build-20260923-2139.log`），产物 grep 确认 §1.4 新结构落地。

### 约定沉淀
- 「论点-佐证」是长节（多子层、多手段）的标准结构化体例，今后写长节默认采用，不必每篇再发明结构。

---

# 2026-09-24 追加

## 体例修正：「论点-佐证」改为「总览 → 手段」三层钻取（用户反馈）

用户指出上一版结构做反了：应是**先总结结论、再围绕结论做结构化佐证**，具体形如：
章先给「总览（子层 / 服务哪一段 / 手段清单表）」→ 子层再给「总览（Network ASCII 图 + 编号手段清单）」→ 每个手段再写「论点 + 佐证」。

### 体系篇（性能优化体系与指标.md）本轮改动
- **§1.4 口径与工具**：重排为 `#### 总览`（口径结论表 + 工具结论表 + 现象→工具对查表 + 通用顺序）+ `#### 口径一/二/三`（实验室 vs 现场 / 分位数与评级 / CLS 双口径）+ `#### 工具一~六`（Network / Performance / Coverage / Memory / Lighthouse / Performance API）。每项都是「**论点**：…／**佐证**：…」。
- **二~四章（12 个子层）全部重排**：`## 章` → `### 总览`（LCP/INP/CLS 构成 ASCII + 章级手段清单表）→ `### X.Y 子层` → `#### 总览`（ASCII 瀑布/流程图 + 编号手段清单 + 判断表）→ 每个手段一个 `#### X`（论点 + 佐证）。
  - 二章手段：2.1 dns-prefetch / preconnect / HTTP/2 / 服务端 TTFB / 资源优先级；2.2 路由级分包 / tree-shaking / 压缩 / 内容哈希；2.3 图片尺寸 / srcset / 懒加载 / 关键 CSS / 字体；2.4 SSR / 骨架屏 / 流式 / 预取。
  - 三章手段：3.1 长任务切片 / Worker / 换算法；3.2 读写分离 / 虚拟滚动 / transform 动画；3.3 稳定 key / 状态下沉 / memo 等 / 重组件按需；3.4 引用释放 / 四类泄漏。
  - 四章手段：4.1 写死尺寸 / 比例一致 / 留位 / 主图不 lazy；4.2 size-adjust / preload；4.3 预留容器 / 不上方插入 / 骨架一致 / 懒加载占位；4.4 transform / 改布局动画 / will-change。
- **所有代码块、示例读数表、ASCII 图逐字保留**（29 对围栏，未新增/删减代码块）——check-code-sync 仍 16/16 全通过。

### 文档组织规范
- `reference/单篇体例.md`：`## 长节用「论点-佐证」结构` 一节**改写为** `## 长节用「总览 → 手段」钻取结构`，骨架换成「章 `### 总览`（手段清单表）→ 子层 `#### 总览`（ASCII+编号清单）→ 手段 `####`（论点+佐证）」；要点强调「三层钻取、结论先行」「证据逐字保留」「注意事项写进论点里，不再单开五段」。

### 校验
- check-code-sync / check-links / check-sidebar-coverage（board frontend）三道全通过（数字同前）。
- VitePress build 重跑，grep 产物确认新结构（`### 总览`、逐手段 `####`）落地。

### 约定沉淀（覆盖前一天那条）
- 长节的标准体例 = **总览 → 手段 的三层钻取**：章总览给结论表，子层总览给图+清单，手段给「论点+佐证」。**不再用 `总纲/结论/观点或佐证/注意事项/事项` 五段骨架**。
