# 2026-09-23 工作日志

## 监控与稳定性 ·「监控平台.md」两处内容修正（用户确认后执行）

**用户提出的两个问题**：① 性能监控指标采集讲了 `performance` 与 `PerformanceObserver`，但没说两者差异、也没给生产推荐；② 1×1 gif 图片打点只列优点、没有代码，应补「不能携带太多数据」的缺点。

**诊断（比用户预期更严重的一处）**：
- ① 行 168–173 把 6 个 API 平铺成一串，未区分「同步快照（拉）」与「异步订阅（推）」；且行 177 的示例用 `observer.observe({ entryTypes: ['navigation'] })` 去听 navigation，与 `性能与体验监控.md` 行 89「导航类指标仍要走 `getEntriesByType`」以及 `sdk/perf.mjs` 的 `readNavigation()` 实现**互相矛盾——错在总览篇**。行 168/169 还把已废弃的 `performance.timing` / `performance.navigation` 当现行 API 列。
- ② `总结.md` 1.4 与 `面试题.md` Q4 其实都写了「只能带很有限的信息」，`前端监控 SDK 实现.md` §5 投递对比表却没有 gif 这一行——**三处口径不齐**，只有总览篇正文漏了缺点。

**改法**（用户选择：写实分工＋换示例 / gif 用示意片段 / 统一三处口径）：
- `监控平台.md` §性能数据接口 拆成「同步快照（拉）」（getEntriesByType / getEntries / now / mark / measure）与「异步订阅（推）」（PerformanceObserver + `buffered: true` + `try/catch`）两组，补生产推荐结论（不是二选一、别轮询、废弃 API 改用 `PerformanceNavigationTiming`）；把错误的 navigation observer 示例换成 `> 摘自 ./code/monitor-lab/sdk/perf.mjs`（取 perf.mjs 第 31–50 行，`observe` + `readNavigation` 恰好连续两段）；`## 配套代码` 表补 `sdk/perf.mjs` 一行。
- `监控平台.md` §上报方式 gif 条目改成 优点 / 缺点 / 定位 三段 + 一段示意片段代码（`new Image()` 打点，带字段截断与时间戳防缓存）；并纠正原文「不会阻塞页面加载」→ 不阻塞首屏，但浏览器为加载图片会**推迟 unload**。
- `总结.md` 1.4 由「三种」改「四种」（补 sendBeacon 一行）；`面试题.md` Q4 展开补 URL 长度上限；`前端监控 SDK 实现.md` §5 表格补「`new Image()` 图片打点」一行。
- 顺手修 `前端监控 SDK 实现.md` 行 80 的历史遗留：正文 `maxQueue: 100,` 比源码多一个尾逗号，check-code-sync 报「代码对不上」（该模块此前从未跑过该校验）。

**事实依据（已联网核实）**：`performance.timing` 已在 Navigation Timing Level 2 废弃，由 `PerformanceNavigationTiming` 取代；`sendBeacon` 队列上限 64 KiB、恒为 POST、返回值只代表「已入队」；图片打点只能 GET、无 body，URL 实际上限约 2 KB（IE 6/7）或 8 KB（Apache 8190、Tomcat/Jetty 8 KB、Akamai/CloudFront/Fastly ~8 KB），且 JSON 拼 URL 有 percent-encoding 膨胀。

**校验（4 件套全绿）**：code-sync 7 篇 / 56 块（38 对齐 + 18 示意，0 问题）· links 706 条 0 问题 · sidebar 188/188 全可达 · build 127.71s 0 告警，9 个产物页 mtime 均晚于对应 md、新正文已进产物。
