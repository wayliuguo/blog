# 2026-09-22 工作日志

## 性能优化模块：体系化重构（8 篇 → 7 篇）

**用户诉求**：模块读起来「蒙」，缺「整体思路 → 拆解 → 核心目标」；CWV 的核心地位没讲透；测量工具独立成篇割裂了「量化」；优化三篇各用各的组织原则串不起来。用户纠结「指标驱动能否取代分层维度」。

**确认的方案（方案C）**：
- 主线：**先量化指标 → 定位瓶颈 → 分层优化 → 持续监控**；核心目标三个 = LCP/INP/CLS（Core Web Vitals），其余是**诊断用的辅助指标**
- 指标驱动作篇章主线，分层（网络/构建/JS/渲染/资源/框架/感知）降为**手段归属标签**，两者用「指标 × 分层」矩阵在速查篇收口——**正交、不互相取代**（理由：一条手段常服务多指标；感知/内存/框架收不进单一指标）

**最终 7 篇**：性能优化总纲与指标量化（原①指标+②工具合并）· 加载性能与 LCP（原③加载+④构建合并）· 交互性能与 INP（原⑤运行时+新增框架层）· 视觉稳定与 CLS（**新写**）· 性能优化闭环 · 优化手段速查（改为矩阵）· Vue SPA 性能实战
删除 5 个旧文件：性能指标与评估 / 性能测量工具实操 / 加载与首屏优化 / 构建侧优化 / 运行时性能

**同步更新**：`.vitepress/config/frontend.js` 侧边栏、`总结.md`（两级重写）、`面试题.md`（章号重映射 + 新增 Q21~Q23 三道 CLS 题，Q24/Q25 为原 Q21/Q22）、`code/perf-lab/README.md` 篇目对照表、`前端框架原理/手写 mini-vue.md` 与 `网络与协议进阶/面试题.md` 的跨模块引用。

**顺带修掉历史遗留**：`性能测量工具实操.md` 的 `timeline()` 代码块被 prettier 折行导致 check-code-sync 报「对不上」，合并时从 `harness/table.mjs` 逐字重建修正。

### 复用经验（第二次做模块体系化重构，与测试体系同一套）
1. **改大 md 一律用 Python 变换脚本**：按 `## ` 切 section → 重新编号拼接 → `io.open(newline='')` 写盘。避免 Write 长文中途损坏，也保证 `> 摘自` 块逐字不动。
2. **裸围栏（``` 无语言）被 check-code-sync 豁免**，只有带语言标签的块才校验 → ASCII 示意图可放心用裸围栏。
3. **先跑 code-sync 拿基线**，能提前发现历史遗留的「对不上」。
4. 正文里若有「见第六节」这类**写在 `> 摘自` 代码块内部**的交叉引用，重排章节时必须让对应内容仍落在第六节，否则改注释会破坏逐字比对。
5. 删旧文件前先用 Python 全仓搜篇名（Grep 工具在大仓会 30s 超时），跨模块引用常藏在别的板块的面试题/导航里。
6. 4 件套：`check-code-sync --board frontend --module 进阶/性能优化` / `check-links` / `check-sidebar-coverage` / `NODE_OPTIONS= node node_modules/vitepress/bin/vitepress.js build`（本次 399s）。
