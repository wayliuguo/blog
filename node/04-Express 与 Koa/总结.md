# 模块总结 · Web 框架基础

## 知识主线

```
01 Express 快速入门
     │  先有 app.use / app.get 这套"中间件流水线"的共同语言
     ▼
02 Koa 快速入门
     │  知道 Koa 只给内核、其余全靠自己装，才明白工程化缺的那层必须自己补
     ▼
03 Express 项目模板
     │  模板里的分层与装配顺序都是"约定"，所以下一步要拆开看底层怎么实现的
     ▼
04 Express 源码分析
     │  Express 底层 = 中间件数组 + 递归 next；把同一套骨架换成 Koa 的洋葱编排
     ▼
05 Koa 项目模板
     │  模板里 errorHandler / logger / responseTime 全靠 await next() 夹出上下半场
     ▼
06 Koa 源码分析
     │  compose + dispatch 说明"前后都能介入"为何成立，两条线在此闭环
     ▼
收口：一个能挂数据库与缓存的 Web 服务骨架
```

本模块的主轴只有一句话：**把 `node:http` 的请求处理抽象成一条中间件流水线**。Express 用"线性数组 + 递归 `next()`"，Koa 用"嵌套 Promise + 洋葱模型"，两条线各自走完「快速入门 → 工程模板 → 源码原理」。

---

## 分篇知识点

### 01-Express 快速入门

- **Express 的定位**：不提供 `node:http` 之外的新能力，只把路由分发、请求体读取、响应封装收敛成约定；手写 `if (req.url === ...)` 变成 `app.get('/user', handler)`
- **Hello World 的五个动作**：`require('express')` → `express()` 建应用 → `app.get('/')` 注册路由 → `res.send()` 输出 → `app.listen(3000)`，5 行起服务
- **路由的两维与占位符**：`app.get/post/put/delete(path, handler)` 按方法与路径两个维度分发，`:id` 是路径里的占位符
- **req.params 恒为字符串**：`/users/123` 命中 `/users/:id` 后 `req.params.id === '123'`，当数字用必须先 `Number()` 转换
- **req.query 与 req.body 的来源差异**：`?page=1` 自动解析进 `req.query.page`；`req.body` 必须挂了 `express.json()` / `urlencoded()` 才存在，`multipart/form-data` 要另上 multer
- **中间件与 next()**：`app.use(fn)` 每个请求都会执行，签名 `(req, res, next)`，只有调用 `next()` 才推进到下一环，否则请求挂起
- **app.use 与 app.get 的匹配差异**：`use` 匹配所有方法且路径是前缀匹配（`/user` 命中 `/user/123`），`get` 只匹配 GET 且路径精确匹配
- **内置与第三方中间件**：`express.json()` / `urlencoded({ extended: true })` / `static('public')` 内置；`cors()` 等跨域、鉴权、限流都要第三方
- **错误处理中间件必须 4 参数**：靠 `fn.length === 4` 识别，链上任意位置 `next(err)` 会跳过后面全部普通中间件，直奔错误处理器
- **Express 与 NestJS 的对比**：Express 无架构约束、TS 可选、无依赖注入；NestJS 强制 MVC / TypeScript / DI / `@Module`，适合中大型长期项目

### 02-Koa 快速入门

- **Koa 的定位**：Express 原班人马打造的继任者，极简内核只给 `ctx` 封装，路由、模板引擎、静态文件、body 解析全部外置
- **Hello World 的写法**：`app.use(async ctx => { ctx.body = 'Hello World' })`，不写 `res.end`，给 `ctx.body` 赋值即完成响应
- **async/await 原生支持**：中间件都是 async 函数，`const data = await database.query(...)` 后直接 `ctx.body = data`，无需回调
- **洋葱模型的顺序**：进入按注册顺序、返回按逆序，两个中间件夹一个处理函数时输出「1 进入 → 2 进入 → 处理请求 → 2 返回 → 1 返回」
- **洋葱成立的前提是 await**：`await next()` 等待的是下游整条链 resolve；写成 `next()` 而不 await，返回段提前执行，洋葱立刻被拉平成线性模型
- **洋葱模型的价值**：请求处理前后都能介入，天然适合请求耗时统计、事务管理、统一响应包装这类跨切面逻辑
- **Express 与 Koa 的七项差异**：中间件模型（线性 / 洋葱）、异步支持（回调 / async）、体积、路由、body 解析、静态文件、社区生态
- **Koa 要额外装的中间件**：`@koa/router` 管路由、`koa-body` 管请求体、`koa-static` 管静态文件，用 `app.use(router.routes())` 挂载

### 03-Express 项目模板

- **分层目录结构**：`src/` 下 config / controllers / entities / middleware / routes / services / utils 七个目录加 `app.js`；单文件服务长成工程骨架
- **package.json 的脚本与依赖栈**：`"dev": "node --watch src/app.js"` 实现免 nodemon 热重启；依赖覆盖 express / cors / dotenv / typeorm / mysql2 / ioredis / jsonwebtoken / bcrypt / joi / winston
- **配置管理的单一出口**：`src/config/index.js` 先 `dotenv.config()` 再聚合 port / database / redis / jwt 三块，业务代码一律不直接读 `process.env`
- **入口文件的中间件装配顺序**：`cors()` → 请求日志 → `express.json()` / `urlencoded()` → 业务路由 → 健康检查 → 404 兜底 → `errorHandler`（必须最后注册）
- **bootstrap 的启动顺序**：先 `AppDataSource.initialize()` 连库、再 `redisClient.ping()` 验缓存、最后 `app.listen()`；任一步失败就 `logger.error` + `process.exit(1)`
- **健康检查与两处兜底**：`GET /health` 用 `SELECT 1` 与 `redisClient.ping()` 双探，任一失败返回 503 + `degraded`；404 用普通中间件，500 交给 4 参数错误处理器
- **TypeORM 的 DataSource 与 EntitySchema**：用 `EntitySchema` 声明 User（email 唯一）与 Product（price decimal(10,2)）两张表；`synchronize` 只在开发环境开，生产走迁移
- **routes / controllers / services 的三层职责**：routes 声明路径与中间件顺序，controller 取请求调 service 再 `res.json`、出错 `next(err)`，service 只管业务规则且完全不碰 `req` / `res`
- **业务错误的统一约定**：service 抛错时打 `err.statusCode` 与 `err.isOperational = true`（409 邮箱已注册、401 密码错误、404 用户不存在），错误处理器据此决定状态码与能否透传文案
- **四个中间件与三个工具模块**：auth 解析 `Bearer` token 写入 `req.user`、validate 用 Joi 校验后 `req.body = value`、errorHandler 区分可预期与未知错误、logger 用 `res.on('finish')` 记耗时；utils 下是 Winston 日志、ioredis 客户端、Joi schemas

### 04-Express 源码分析

- **Express 的核心只有三件事**：路由注册、中间件链、响应封装；不到 100 行就能写出能跑的最小版本
- **两个核心数据结构**：`middlewares = [{ path, handler }]` 由 `app.use` 填充，`routes = [{ method, path, handler }]` 由 `app.get` 等方法填充
- **app.use 的可选路径重载**：靠 `handler === undefined` 区分 `app.use(fn)` 与 `app.use(path, fn)`，前者补上 `path = '/'`
- **动态路由转正则**：`/users/:id` 把 `:id` 替换成 `([^/]+)` 拼成 `^/users/([^/]+)$`，`url.match(regex)` 后按顺序把捕获组写回 `req.params`
- **中间件链的线性递归**：`executeMiddlewareChain` 内维护 `let index = 0`，每次 `next()` 取走 `middlewareList[index++]`；走到末尾就执行回调（路由匹配阶段）
- **中间件的前缀匹配**：`mw.path !== '/' && !req.url.startsWith(mw.path)` 时直接 `next()` 跳过，这是最小实现里唯一的路径判断
- **next 的双重语义**：`next` 既是"推进下一个中间件"，也是"错误传递通道"——`next(err)` 有值时跳过剩余中间件直接调 `errorHandler`，没注册处理器就回落 500
- **enhanceRes 增强响应对象**：给原生 `res` 挂 `status(code)`（设状态码并 `return res` 支持链式）、`json(data)`（设 JSON 头 + `end`）、`send(body)`（对象走 json，其余走 text/html）
- **同步 try/catch 的能力边界**：最小实现里普通中间件与路由 handler 都包在 `try/catch` 内并 `next(err)`，所以只能接住**同步**抛出的异常
- **最小实现 vs Express 源码**：真源码有 `Router` 类（子路由、路由级中间件、参数解码）、Layer 缓存与路由压缩（trie / radix tree），不是每次请求线性遍历；"100 行跑通"不等于"100 行能上生产"

### 05-Koa 项目模板

- **目录结构与 Express 模板的差异**：分层同名同构，只多一个 `middleware/responseTime.js`，因为洋葱模型让"耗时统计"能顺手写成独立中间件
- **package.json 的依赖替换**：koa / @koa/router / koa-body / @koa/cors 替掉 express / cors，其余依赖栈（typeorm + mysql2 + ioredis + jsonwebtoken + bcrypt + joi + winston）与 Express 模板完全一致
- **@koa/router 的前缀挂载**：`new Router({ prefix: '/api' })` 统一前缀，子路由用 `api.use(userRoutes.routes())` 挂上，最后 `app.use(api.routes())` + `app.use(api.allowedMethods())`
- **入口的洋葱堆叠顺序**：`cors()` → `errorHandler` → `responseTime` → `logger` → `koaBody()` → 路由 → 健康检查；越先注册越靠外层，所以错误处理能包住整条链
- **errorHandler 靠 try/catch 包链**：最外层中间件 `try { await next() } catch (err)`，按 `err.statusCode || err.status || 500` 写 `ctx.status`，最后 `ctx.app.emit('error', err, ctx)` 交给应用级日志
- **logger 与 responseTime 是洋葱的典型用法**：进入时记 `start`，`await next()` 之后才算差值、写响应头，这正是线性模型做不到的"响应的上半场"
- **ctx.state 在中间件间传数据**：auth 中间件把解码结果写到 `ctx.state.user`，控制器读 `ctx.state.user.userId`；等价于 Express 挂在 `req.user` 上
- **控制器不用逐个 try/catch**：Koa 控制器直接 `await userService.xxx()`，异常顺着 `await next()` 的 Promise rejection 回到最外层统一处理，这是两端模板写法差异的根因
- **业务错误约定与 Express 保持一致**：服务层依旧抛 `statusCode` + `isOperational`，所以同一套错误判据能同时用在两套模板上
- **404 与真错误必须分开**：没有路由命中时什么都没抛，`catch` 不会触发；自定义 404 要靠路由之后的兜底判断，或用 `allowedMethods()` 统一处理未匹配的方法与路径

### 06-Koa 源码分析

- **Koa 的核心只有两件事**：洋葱模型中间件链（`compose`）与 Context 封装（`createContext`），比 Express 更精简
- **app.use 支持链式调用**：`middlewares.push(fn)` 之后 `return app`，所以能连续 `app.use(...).use(...)`
- **compose 的结构与递归终点**：返回 `function (ctx)`，内部 `let index = -1` 后从 `dispatch(0)` 启动；`const fn = middlewareList[i]; if (!fn) return Promise.resolve()` 是递归的自然终点
- **dispatch 把下一个自己当作 next**：`Promise.resolve(fn(ctx, () => dispatch(i + 1)))`——`await next()` 之所以能等到下游完成，全靠这一句把 `dispatch(i+1)` 包成 Promise 返回
- **index 用于防重入**：每次 `dispatch(i)` 先判 `if (i <= index)` 就 `reject(new Error('next() 被多次调用'))`，再 `index = i`；同一中间件里写两个 `await next()` 会直接报错而不是把下游重跑一遍
- **await next() 决定回程顺序**：`await` 等到的是下游整条链的 Promise resolve，所以 A、B 的"返回段"只能在内层 C 结束之后才开始执行
- **createContext 封装 ctx**：挂上 method / url / path（`?` 之前）/ query / headers / status / body 与 `ctx.set()`；`ctx.body` 的 setter 在赋值时标记 `_respond`
- **respond 按类型自动响应**：string → `text/html; charset=utf-8`；对象 → `application/json` + `JSON.stringify`；Buffer → 原样 `end`；`null` / `undefined` → 状态码改成 **204** 且不发 body
- **错误统一靠 Promise catch**：`fn(ctx).then(respond).catch(...)`，任意中间件 reject 都落到同一个 catch 里返回 500
- **最小实现 vs Koa 源码**：真源码用 `koa-compose` 包、ctx 的 request / response 双层属性委托、`ctx.onerror` 与 `app.on('error')` 事件、支持 stream 作为 `ctx.body` 与文件上传，路由则交给 `@koa/router`

---

## 核心速查表

| 知识点 | 一句话结论 | 出处 |
| --- | --- | --- |
| Express 的定位 | 不提供新能力，只把 `node:http` 的路由分发、body 读取、响应封装收敛成约定 | 01 篇 |
| app.use vs app.get | `use` 匹配所有方法且前缀匹配，`get` 只匹配 GET 且精确匹配 | 01 篇 |
| req.params | 路径占位符提取而来，恒为字符串，当数字用必须先转换 | 01 篇 |
| req.body 的前提 | 必须挂 `express.json()` / `urlencoded()` 才存在；`multipart` 要另上 multer | 01 篇 |
| 错误处理中间件 | 靠 `fn.length === 4` 识别，`next(err)` 会跳过后面全部普通中间件 | 01 篇 |
| next() 不调用的后果 | 请求挂起，既不推进也不响应，客户端一直等到超时 | 01 篇 |
| Koa 的定位 | 极简内核只给 `ctx`，路由、body、静态文件全部外置 | 02 篇 |
| 洋葱模型 | 进入按注册顺序，返回按逆序 | 02 篇 |
| 洋葱成立的前提 | 必须 `await next()`；写成 `next()` 就退化成线性模型 | 02 篇 |
| 配置管理 | dotenv 读 `.env` 后由 `config/index.js` 聚合，业务不直接读 `process.env` | 03 篇 |
| 中间件注册顺序 | cors → 日志 → body 解析 → 路由 → 404 兜底 → errorHandler（最后） | 03 篇 |
| bootstrap 启动顺序 | 先连库、再验 Redis、最后 listen；失败即 `process.exit(1)` | 03 篇 |
| 三层职责 | routes 管路径与顺序、controllers 管取参与输出、services 管业务且不碰 req/res | 03 篇 |
| 业务错误约定 | 抛错打 `statusCode` + `isOperational`，决定状态码与能否透传文案 | 03 篇 |
| Express 最小实现核心 | 中间件数组 + 一次递归 `next` + 路由正则匹配 + 增强 `res` | 04 篇 |
| 动态路由转正则 | `/users/:id` → `^/users/([^/]+)$`，捕获组按序写回 `req.params` | 04 篇 |
| next 的双重语义 | 既推进下一个中间件，也是错误传递通道 | 04 篇 |
| 与真源码的差距 | 真源码有 `Router` 类、Layer 缓存、路由压缩，不是每次请求线性遍历 | 04 篇 |
| Koa 入口洋葱顺序 | cors → errorHandler → responseTime → logger → koaBody → 路由 | 05 篇 |
| Koa 错误处理 | 最外层 `try { await next() } catch` 包链，`ctx.throw` 抛带 status 的 HttpError | 05 篇 |
| ctx.state | 中间件间传数据，auth 写 `ctx.state.user`，等价 Express 的 `req.user` | 05 篇 |
| compose 的本质 | 把中间件数组组合成嵌套 Promise 链，`dispatch(i)` 递归推进 | 06 篇 |
| dispatch 的 next | `Promise.resolve(fn(ctx, () => dispatch(i + 1)))` 是洋葱成立的关键 | 06 篇 |
| index 防重入 | `i <= index` 即 reject「next() 被多次调用」 | 06 篇 |
| ctx.body 类型映射 | string → text/html、对象 → JSON、Buffer → 原样、`null` → 204 | 06 篇 |

---

## 见 X 想 Y

现象是表相，先定位到层，再动手。

| 见到什么 | 该想到什么 | 一句话理由 |
| --- | --- | --- |
| 接口一直无响应，日志只有进入没有结束 | 中间件既没调 `next()` 也没发响应 | 链靠 `next()` 递归推进，断掉就既不推进也不响应 |
| 控制台里「A 返回」排在「B 进入」之前 | Koa 中间件里漏了 `await` | 不 await 就等不到下游链 resolve，回程段提前执行 |
| async 中间件抛错，错误处理中间件毫无动静 | Express 4 只能接住同步异常 | async 返回的是 Promise，reject 不会自动转成 `next(err)` |
| 出错时浏览器只报跨域，看不到真实状态码 | CORS 中间件注册得太靠后 | 错误响应没带上跨域头，真实错误被掩盖 |
| 新加的路由仍返回 404 兜底文案 | 路由注册在 404 兜底之后 | 中间件按注册顺序执行，越早注册越靠外层 |
| 错误处理器写在路由前，后面抛的错它看不见 | 错误处理器必须最后注册 | 只有排在所有路由之后的 4 参数函数才能兜住全部 `next(err)` |
| `req.params.id + 1` 得到 `"11"` | 路径参数恒为字符串 | 提取自 URL 文本，参与运算前需显式转换 |
| 想在某接口打出「进入 + 耗时」两行日志 | 这是洋葱模型 / `res.on('finish')` 的用武之地 | Koa 用 `await next()` 前后夹，Express 靠响应结束事件 |
| 生产响应里出现了 SQL 片段或文件路径 | 非预期错误的 `message` 被直接回传 | 只有 `isOperational` 的业务错误才适合透传文案 |
| 想把同一份 service 给定时任务复用 | 先检查它有没有碰 `req` / `res` | 服务层不感知 HTTP 才有复用与单测的可能 |
| 路由改成 `/users/:id` 后 `req.params` 是空对象 | 路由没命中，或正则未匹配上 | `:id` 先转 `([^/]+)` 再 match，命不中就没有 params |
| `await next()` 之后还想改响应内容 | 最终以最后一次赋值为准 | `ctx.body` 赋值只标记待发送，真正 `respond` 在链尾 |
| `ctx.body = null` 却拿到 204 空响应 | 这是 Koa 的约定而非 bug | `null` / `undefined` 会被映射成 204 且不发送 body |
| 同一个中间件里写了两个 `await next()` | compose 会直接抛「next() 被多次调用」 | `index` 记录已执行的 dispatch 编号，用来防重入 |

---

## 易错点汇总

- **"Express 提供了原生 `http` 没有的能力"**：Express 不新增能力，只是把 `node:http` 的常用写法标准化。理解这一点才能看懂最小实现为何不到 100 行就能跑通，也才不会在源码里找"黑魔法"。
- **"Koa 的洋葱形态是注册时就形成的"**：形态取决于代码里有没有 `await`。写 `next()` 而不 await，返回段会提前执行，洋葱立刻被拉平成线性模型，日志与耗时统计的结果跟着全乱。
- **"Express 的 async 中间件抛错会被错误处理中间件接住"**：Express 4 只在同步调用栈里 `try/catch`，async 函数内部的 reject 不会自动转成 `next(err)`。三种修法：中间件内 `try/catch` 后 `next(err)`、包一层 `asyncHandler` 自动 `.catch(next)`、升级到 Express 5。
- **"中间件不调 `next()` 一定是 bug"**：终止型中间件（路由处理函数、鉴权失败分支）不调是正常的，前提是已经用 `res.json()` / `res.send()` 结束了响应；"既不响应也不推进"才是 bug，表现为请求挂起、`res.on('finish')` 不触发。
- **"错误处理中间件少写一个参数也行"**：Express 靠 `fn.length === 4` 识别，四个形参必须全部写出来。省掉任意一个就退化成普通中间件，错误被静默放过，表现为请求挂起而不是报错。
- **"`req.params` 拿到的是数字"**：路径参数提取自 URL 文本，恒为字符串。`req.params.id + 1` 得到的是 `"11"` 而不是 `2`，必须先 `Number()` 或 `parseInt()`。
- **"不挂 body 解析中间件也能读 `req.body`"**：`req.body` 依赖 `express.json()` / `urlencoded()`；`multipart/form-data` 内置解析器不支持，要另上 `multer` 之类的中间件。
- **"CORS 中间件放哪都行"**：必须放在路由之前，保证任何响应（含错误响应）都带上跨域头。否则出错时浏览器只报跨域，真实状态码与错误信息被完全掩盖。
- **"把 `req` / `res` 传进 service 层很方便"**：一旦在 service 里写 `res.json()`，这个复用点就没了，单测也得靠 mock 请求对象。service 不感知 HTTP 才是这条分层的价值所在。
- **"404 是错误，应该由错误处理器处理"**：没有路由命中时什么都没抛，`catch` 根本不触发。想自定义 404 文案就得在路由之后补一层判断，或用 `allowedMethods()` 统一处理。
- **"生产环境回 `err.message` 更利于排查"**：非预期错误的 `message` 可能夹带 SQL 片段、文件路径、内部字段名，原样返回就是信息泄露。只有可预期的业务错误才适合透传，`stack` 也只在开发环境附加。
- **"同一个中间件里可以多次 `await next()`"**：compose 的 `index` 就是防这个的，第二次调用会直接 reject，而不是把下游中间件重跑一遍——这是最容易写出"重复响应头"或"重复写库"的坑。

---

## 与相邻模块的接口

**进入本模块前必须已经掌握**

上一模块是**模块二 · 网络底层**，它给了 `node:http` 那一层：用 `http.createServer` 拿回调、路由靠 `if (req.url === ...)` 分发、请求体靠 `chunks.push` + `Buffer.concat` 读取、响应靠 `res.setHeader` + `res.end` 输出，以及两个前提——`req` 是可读流、`res` 是可写流。入口见 [模块二 · 网络底层总结](../03-网络编程与实时通信/总结.md)。

本模块所有的"抽象"都建立在这之上：Express 就是把上面这些重复劳动收敛成路由注册、中间件链、响应封装；Koa 再把它换成 `ctx` 封装加 Promise 化的洋葱链。没有模块二的 `http` 心智模型，读 04 / 06 两篇的最小实现只会觉得是在凭空空想。

**本模块交给下一模块什么**

本模块产出一个**能挂数据库与缓存的 Web 服务骨架**：`routes / controllers / services / entities` 四层分工 + `config` 统一聚合配置 + 五道中间件（cors / logger / auth / validate / errorHandler）+ `bootstrap` 的启动顺序（先连库、再验缓存、最后 listen），以及 404 / 401 / 500 的统一处理口径。

下一模块是**模块四 · 数据与缓存**，它要靠这套骨架做什么：把模板里的 `TypeORM DataSource` 与 `ioredis` 从"能连通"推进到"会用"——SQL 建表与 JOIN、索引与事务、缓存穿透 / 击穿 / 雪崩、缓存与数据库的一致性，全部挂在这套骨架的 service 层上。出口见 [模块四 · 数据与缓存总结](../05-数据库/总结.md)。

---

## 代码与自测入口

本模块配套代码共 4 个项目，都在本目录 `code/` 下，路径为纯文本：

- `node/04-Express 与 Koa/code/express-mini` —— 200 行还原 Express 核心（中间件链、路由匹配、`res` 封装）。无第三方依赖，`npm start` 或直接 `node index.js`，默认监听 3000；`curl http://localhost:3000/users/42` 可验证动态路由参数。
- `node/04-Express 与 Koa/code/koa-mini` —— 180 行还原 Koa 洋葱模型（`compose` / `dispatch` / `ctx`）。同样无依赖，`npm start` 后 `curl http://localhost:3000/health` 返回健康检查结果。
- `node/04-Express 与 Koa/code/express-template` —— Express + TypeORM + Redis + JWT 模板。需 MySQL 与 Redis 可达，依次 `npm install` → `npm run migration:run` 建表 → `npm run dev`。
- `node/04-Express 与 Koa/code/koa-template` —— 同栈的 Koa 版本。读 `src/app.js` 里中间件从上到下的堆叠顺序（cors → errorHandler → responseTime → logger → koaBody → 路由），最能体会洋葱模型。

本模块面试题：[模块面试题](./面试题.md)。各篇末尾的 `## 参考` 里也有回到本页的入口。

---

## 参考

- 本模块篇目：[01-Express 快速入门](./01-Express%20快速入门.md) · [02-Koa 快速入门](./02-Koa%20快速入门.md) · [03-Express 项目模板](./03-Express%20项目模板.md) · [04-Express 源码分析](./04-Express%20源码分析.md) · [05-Koa 项目模板](./05-Koa%20项目模板.md) · [06-Koa 源码分析](./06-Koa%20源码分析.md)
- [模块面试题](./面试题.md)
- 上一模块：[模块二 · 网络底层总结](../03-网络编程与实时通信/总结.md)
- 下一模块：[模块四 · 数据与缓存总结](../05-数据库/总结.md)
