# 测试

---

## [中级] 测试金字塔

```
        ⬆
    E2E 测试  ⬆ 少
  集成测试  ⬆
单元测试  ⬆ 多
```

| 测试类型 | 速度 | 数量 | 覆盖范围 |
|---------|------|------|---------|
| 单元测试 | 极快 | 最多 | 单个函数/方法 |
| 集成测试 | 快 | 中等 | 模块间交互 |
| E2E 测试 | 慢 | 最少 | 完整请求链路 |

## [中级] NestJS 测试

### 单元测试

```typescript
import { Test, TestingModule } from '@nestjs/testing'

describe('UserService', () => {
    let service: UserService

    beforeEach(async () => {
        const module: TestingModule = await Test.createTestingModule({
            providers: [UserService],
        }).compile()

        service = module.get<UserService>(UserService)
    })

    it('should return user by id', async () => {
        const user = await service.findById(1)
        expect(user).toBeDefined()
        expect(user.id).toBe(1)
    })
})
```

### 集成测试

```typescript
describe('UserController', () => {
    let app: INestApplication

    beforeAll(async () => {
        const moduleFixture = await Test.createTestingModule({
            imports: [AppModule],
        }).compile()

        app = moduleFixture.createNestApplication()
        await app.init()
    })

    it('/users (GET)', async () => {
        const response = await request(app.getHttpServer())
            .get('/users')
            .expect(200)

        expect(response.body).toBeInstanceOf(Array)
    })
})
```

## [中级] Mock / Stub / Spy

```typescript
// Mock：模拟整个对象
const mockUserService = {
    findById: jest.fn().mockResolvedValue({ id: 1, name: '张三' })
}

// Stub：替换方法返回值
jest.spyOn(userService, 'findById').mockResolvedValue({ id: 1, name: '张三' })

// Spy：监控方法调用
const spy = jest.spyOn(userService, 'findById')
userService.findById(1)
expect(spy).toHaveBeenCalledWith(1)
```

---

## 面试题

### Q1: 单元测试和集成测试的区别？

单元测试测试单个函数/方法，速度快，不依赖外部服务（数据库、网络等）。集成测试测试模块间交互，需要启动 NestJS 应用上下文，速度较慢。

### Q2: Mock 和 Stub 的区别？

Mock 模拟整个对象的行为，验证方法是否被正确调用。Stub 替换方法的返回值，关注测试结果的正确性。Spy 在真实对象上监听方法调用。

---

## 参考

- 上一篇：[安全](./03-安全)
- 下一篇：[设计模式](./05-设计模式)