# 模块总结 · 网络底层

## 知识主线

本模块 5 篇不是并列的知识点，而是一条逐层叠加语义的链：先立坐标，再摸最底层的字节，然后一层层往上加"语义"，最后纵向装一次。

```
模块二 · 网络底层：五篇的依赖关系

导读与全景图
   先立坐标：一张「浏览器 → Controller」链路图 + 一张分层故障现象表
        │
        │ 坐标有了，先摸最底下那段：字节到底是怎么进来的
        ▼
01 TCP 与 Socket 编程
   TCP 是可靠字节流，Socket 是它的双工流外壳，epoll 是盯着它的那只手
        │
        │ 管道只是管道，"GET /users" 这层语义还没人定义
        ▼
02 HTTP 与 HTTPS 深入
   在 TCP 之上约定报文格式，req / res 顺手沿用流；HTTPS 再垫一层 TLS
        │
        │ 一问一答的连接活不长，服务端没法主动开口
        ▼
03 WebSocket 与 SSE 实时通信
   把连接留成常开：一条双向全双工，一条单向只推；心跳负责剔除假死
        │
        │ 零件齐了，纵向装一次，看哪一步断了会是什么现象
        ▼
04 一次请求完整经历了什么
   31 步链路 + 排障坐标系 + 框架到底封了什么，回答"为什么还要学这一层"
```

主轴一句话：**字节怎么从网卡走到你的业务函数，又怎么原路走回去**——从裸 TCP 的一段字节流开始，依次补上消息边界、请求语义、长连接形态，最后纵着装成一条可定位、可排障的链路。

## 分篇知识点

### 导读与全景图

- **学习动机**：框架把整条链路藏起来时，你仍要懂底下发生了什么——学网络层不是为了裸写 TCP，而是拿到「框架之下的坐标系」；线上问题往往在最下面几层，懂这一层才知道往哪看。
- **五条暗线（模块一机制在网络层的真实来源）**：Event Loop 等的是内核 I/O 就绪通知；libuv 屏蔽 epoll/kqueue/IOCP 差异；Buffer 是字节流进出 Node 的第一手容器；`net.Socket` 即 Duplex Stream（req/res 之流的根源）；单线程靠内核唤醒而非死守连接。
- **网络层排在运行环境之后是依赖不是难易**：模块一讲的 Event Loop / libuv / Buffer / Stream，正好在网络层各自找到"真实来源"。
- **全景图**：Browser → HTTP → TLS → TCP → IP → 网卡 → 内核 → Socket → epoll → libuv → Event Loop → node:http → 框架 → Controller。
- **链路是对称的**：响应沿原路反向再走一遍，"请求怎么进来"和"响应怎么出去"是同一张图的两次遍历。
- **用户态与内核态的分界**：libuv 往上是你写代码能控制的范围，内核及其以下（Socket 缓冲区、TCP、epoll）改 JS 改不动。
- **框架替你做的三件事**：把字节流切成请求、把写响应封成 `res.json()`、把连接生命周期管起来——这三件一出问题，盲区就变成故障点。
- **五篇路线图**：导读：立坐标 → 01 字节流 → 02 请求语义 → 03 长连接 → 04 纵向收束；建议前四篇边读边跑，04 篇不引入新实验。
- **每层出问题的典型现象表**：DNS 层 `ENOTFOUND` / `EAI_AGAIN`，TCP 层 `ECONNREFUSED` / `ECONNRESET` / `ETIMEDOUT` / `EADDRINUSE`，TLS 层证书校验失败，HTTP 层 400 / 404 / 431，网关层 502 / 504，运行时层 `Too many open files`。
- **报错越靠上越像配置与路由问题，越靠下越像连接生命周期与资源问题**：先定位层，再决定查什么，不要一上来全局搜代码。
- **这一层最易被跳过，回报却最高**：框架把它包得太好，你几乎感觉不到它存在；但读 Express 最小实现或 NestJS 请求处理链时"有没有感觉"，全看这一层。
- **边界声明**：不深入内核协议栈源码、不手写 epoll、不背 TCP 状态机；目标只是出问题时知道往哪看、读框架时知道底下是什么。


### 01 TCP 与 Socket 编程

- **TCP/IP 分层模型**
  - 四层：应用层（HTTP/HTTPS/WebSocket/业务协议）→ 传输层（TCP 可靠有序 / UDP 快不可靠）→ 网络层（IP 寻址+路由）→ 网络接口层（Ethernet/Wi-Fi）；每层只解决一件事，排障先定位故障落在哪一层
  - 请求下降链路：`GET /users` 属应用层，HTTP 只定义报文不管送达 → TCP 可靠有序送达 → IP 寻址路由 → 网卡变电信号
- **TCP 的三重保证与三次握手**
  1. 面向连接：通信前三次握手建立连接，否则对方可能没准备好
  2. 可靠：丢包重传、校验和、确认应答，否则文件残缺
  3. 有序：序号保证到达顺序，否则消息乱序
  - 不能省成两次：两次握手时服务端收到 `SYN` 就分配资源，滞留的旧报文会让它空等超时（SYN Flood 即利用此）；第三次握手才确认客户端已收到回应
- **字节流不是消息流**
  - `data` 是 `Buffer`：网络上流动的是字节，`socket.on('data')` 拿到的是 Buffer，需手动 `.toString('utf8')` 才是字符串
  - 粘包/拆包：`hello`+`world` 可能粘成一次收到或拆成多次；TCP 只保证可靠有序，不认业务消息边界
  - 边界方案：长度前缀（`Length(4B)+Body(NB)`）最通用，不依赖分隔符、不浪费补齐字节、对二进制同样适用；定长与分隔符（`\n`）次之；HTTP 用 `Content-Length` 自定边界
- **Socket：OS 网络能力的接口抽象**
  - 四元组唯一区分连接（`源IP:源Port → 目标IP:目标Port`），服务端固定端口、客户端端口随机复用，故单机可撑万连接
  - `node:net` 是最底层入口：处理 `data`/`end`/`error` 三类事件
  - `net.Socket` 是 Duplex Stream（同时 Readable+Writable），收发可并行不阻塞，即 TCP 全双工的体现
- **内核、epoll 与 libuv 的分工**
  - 盯着 Socket 的是内核：10,000 个 Socket 忙轮询低效，Linux `epoll` 让内核主动通知就绪
  - libuv 抹平平台差异：Linux `epoll` / macOS `kqueue` / Windows `IOCP` 抽象成统一异步接口
  - 单线程扛万连接：等待交给内核与 libuv，主线程只处理就绪事件——"等待不占线程"
  - 网络 I/O 不走线程池：`net`/`http`/`https` 走 OS 事件通知、主线程回调；只有文件 I/O、DNS、crypto、zlib 等才进线程池，不要记成"异步=线程池"
- **背压：`write()` 返回值与 `drain`**
  - 生产远快于消费会积压内存直至 OOM；`socket.write()` 返回 `false` 表示缓冲到阈值，应等 `drain` 再继续写
- **`pipe` / `pipeline`：别自己接背压**
  - 反例：`on('data') + write` 丢掉背压，返回 `false` 也照写
  - `pipe()` 内部协调快慢，下游跟不上就暂停上游；`pipeline()` 出错能统一销毁整条链路，生产更推荐


### 02 HTTP 与 HTTPS 深入

- **HTTP 是 TCP 之上的应用层协议**
  - 约定 `Method`/`URL`/`Header`/`Body`/`Status Code`，头与体靠空行分界，体的结束靠 `Content-Length` 或 `Transfer-Encoding: chunked`
  - 抽象层级：`node:http` → Express/Fastify → NestJS，框架已按 HTTP 规则把字节流解析好（所以能直接拿到 `@Body()`）
  - 手写 `node:http` 会撞上的问题：路由分发、查询参数解析、Body 读取、统一兜底、横切鉴权、分层组织——Express/Fastify 就是来收这些的
- **`req` 与 `res` 本质上也是流**
  - `req` 是 Readable（请求体分多次到达，`chunk` 是 Buffer，靠 `data`+`end`+`Buffer.concat` 拼回字符串）
  - `res` 是 Writable（可多次 `write`，最后 `end` 收尾）
  - 收益：大文件下载可直接 `readableStream.pipe(res)`，边读边发，不必先读进内存
- **流式响应的边界：Content-Length 与 chunked**
  - 一次性算出的响应体可在 `writeHead` 带 `Content-Length`，但必须用 `Buffer.byteLength()`（字节长度，非字符长度——`'你好'` 字符长 2、字节长 6）
  - 流式响应算不出总长度，改用 `Transfer-Encoding: chunked` 分块传输，实现"边产生边发送边结束"
- **Keep-Alive 与三个超时**
  - 价值：多个 HTTP 请求复用同一条 TCP 连接，省掉每次的握手/挥手成本
  - 分工：`keepAliveTimeout` 管空闲多久关连接；`headersTimeout` 管收齐请求头时限；`requestTimeout` 管单请求总时限
  - 硬约束：`headersTimeout` 必须 > `keepAliveTimeout`，否则空闲连接被关时后续请求被丢，客户端见 `ECONNRESET`/`socket hang up`
- **HTTPS 与 TLS 解决的问题**
  - TLS 解决三件事：加密（防中间人窃听明文）、身份认证（证书防伪造钓鱼）、数据完整性（防响应被篡改注入）
  - HTTPS 不是新协议，而是 HTTP over TLS（握手+加密+完整性校验）；生产里 TLS 通常在网关卸载，Node 跑内部明文 HTTP
- **HTTP 版本演进：队头阻塞与多路复用**
  - HTTP/1.1：同一条连接上请求/响应串行（应用层队头阻塞），靠开多条 TCP 连接绕过
  - HTTP/2：单条 TCP 连接上多 Stream 交错（多路复用），加二进制分帧与 HPACK 头压缩；但仍共享一条 TCP，丢包会触发 TCP 层队头阻塞
  - HTTP/3：传输层换成基于 UDP 的 QUIC，各 Stream 更独立，丢包只影响自身
- **报错码与排障方向**
  - `ECONNRESET`：对端突然重置（keep-alive 连接被服务端先关，或读写时对方已断）
  - `socket hang up`：客户端视角连接被对方提前关闭
  - `ECONNREFUSED`：目标地址/端口无进程监听
  - `431`：请求头过大（Cookie/鉴权头累积）
  - `502` vs `504`：前者网关根本没连上上游，后者连上了但上游迟迟不回（慢查询/下游阻塞）
  - 方法论：遇网络报错先定位"断在哪一跳"，再看对应超时与配置


### 03 WebSocket 与 SSE 实时通信

- **WebSocket：实时双向通信的应用层协议**
  - 定位：HTTP 一问一答不匹配"服务端随时主动推"，WebSocket 为这类双向实时场景而生
  - 不替代 TCP：分层 `WebSocket → TCP → IP`；裸 TCP 要自己定消息格式/分包/心跳，WebSocket 已标准化 Handshake/Frame/Message/Ping-Pong/Close/Text-Binary
- **建立连接：先 HTTP 升级，不新建连接**
  - 客户端带 `Upgrade: websocket` + `Sec-WebSocket-Key`，服务端回 `101 Switching Protocols` 后接管同一条 TCP 连接
  - `Sec-WebSocket-Accept` 不是加密：把 `Key` 拼固定串做 SHA-1+Base64 回传，仅用于证明对方真懂握手、防代理误判
- **长连接的心跳与生产必备件**
  - 为什么心跳：客户端断网/进程被杀/NAT 超时后服务端不能立刻感知，连接"假死"会吃光内存与 fd；`Ping/Pong` 多次无响应即判失效释放
  - 生产必备：心跳、断线感知、客户端重连、超时关闭、健康度维护、消息确认、扩容分布
  - 多实例：连接散落不同进程，全房间广播需 Redis 发布订阅/消息总线或集中式连接管理——成熟项目直接用带集群能力的库
- **SSE：单向推送**
  - 方向固定 `Client ← Server`，用普通 HTTP 长连接；报文纯文本、空行分隔事件，`event`/`data`/`id`/`retry` 四个字段
  - 断线续传：浏览器 `EventSource` 自动重连并带 `Last-Event-ID`，服务端从断点续推
  - Node 侧三步：`Content-Type: text/event-stream` → 关默认缓冲（`Cache-Control: no-cache`、`Connection: keep-alive`）→ 往 `res` 流持续 `write`，并监听 `req.on('close')` 清定时器
- **流式输出场景适合 SSE**
  - 对照：20 秒任务若等算完再返回，用户干等像卡死；边产生边推则立即显示
  - 典型：日志回放、任务进度、大文件生成、实时监控——共同点是数据分段来且不需客户端高频回传
- **SSE 与 WebSocket 的选型准则**
  - SSE 单向、仅文本、普通 HTTP、浏览器内置重连、网关易配；WebSocket 双向全双工、原生 Text/Binary、需配 `Upgrade`、重连自实现
  - 判断：业务本质是 `Server → Client` 持续推送（通知/行情/日志/进度）→ SSE 足够；需 `Client ⇄ Server` 高频双向（聊天/协作/游戏）→ WebSocket


### 04 一次请求完整经历了什么

- **31 步链路**：DNS → 三次握手 → [TLS] → 组报文 → 内核协议栈 → 网卡 → 路由 → 服务端 Receive Buffer → fd 变 readable → epoll → libuv → poll → `node:http` 解析 → `req` / `res` → 框架 → Controller。
- **你那行 `return []` 只是第 30 步**：前面 29 步都在"把请求安全送到这一行"，最后 1 步是"把结果安全送回去"。
- **第 18–22 步是 Node 网络能力的真正底座**：Receive Buffer → fd 就绪 → epoll 通知 → libuv 取事件 → poll 阶段消费，这才是"单线程扛万连接"的落点。
- **排障坐标系**：DNS 1–2 步看 `ENOTFOUND` / `EAI_AGAIN`；TCP 4–6 步看 `ECONNREFUSED` / `ETIMEDOUT`；TLS 7–11 步看证书链与系统时间；网关 16 步看 502 / 504；Keep-Alive 影响第 31 步的 `ECONNRESET`。
- **资源层与运行时层的两类现象**：`EMFILE` / `Too many open files` 是 fd 耗尽（连接泄漏或真高并发）；"CPU 高但 QPS 低"是事件循环被同步任务占住。
- **响应序列化也是故障点**：一次性 `JSON.stringify` 超大对象会卡住主线程并涨内存，应改流式、分页或压缩。
- **NestJS 封装了 `node:http` 之上的一切**：Routing、Controller、Provider / DI、Middleware、Guard、Interceptor、Pipe、Exception Filter、WebSocket Gateway。
- **框架的"两层不知道"**：框架不知道自己底下有 epoll 与事件循环，最后都落到一次 `res.end()`；只学框架的你也不知道请求先在 Receive Buffer 排队、被标记可读才触发。
- **为什么还要学这一层**：写业务时代码永远待在最上面一层，出问题时问题往往在最下面几层——学底层是把"报错"翻译成"该去哪一层看"。
- **一条可验证的是否判断链**：DNS 正常吗 → TCP 能建连吗 → TLS 握手成功吗 → 网关正常吗 → Keep-Alive 有问题吗 → Node 服务正常吗 → 事件循环被阻塞吗 → 框架报错吗 → 业务异常吗。

## 核心速查表

取本模块最容易被追问的知识点，出处写篇号，便于回查正文。

| 知识点 | 一句话结论 | 出处 |
| --- | --- | --- |
| 分层故障坐标系 | 报错越靠上越像配置 / 路由问题，越靠下越像连接生命周期与资源问题 | 导读 |
| TCP 三重保证 | 面向连接 + 可靠 + 有序，缺一条就是下载残缺或消息乱序 | 01 篇 |
| 三次握手 | 第三次 ACK 让服务端确认"客户端收到了我的回应"才敢分配资源，省成两次会被滞留 SYN 白占资源 | 01 篇 |
| TCP 是字节流 | `write('hello')` + `write('world')` 可能并成一次 `data`，消息边界要应用层自己定义 | 01 篇 |
| 长度前缀分包 | `4 字节长度 + N 字节 Body`，不依赖内容里是否恰好出现分隔符，二进制数据也适用 | 01 篇 |
| Socket 四元组 | 源 IP:Port → 目标 IP:Port 唯一区分一条连接，源端口不同即不同连接 | 01 篇 |
| `net.Socket` 是 Duplex Stream | 可读 `on('data')` + 可写 `write()`，这是 `req` / `res` 是流的根源 | 01 篇 |
| epoll | 把上万 fd 交给内核关注、就绪时主动通知，取代逐个忙轮询 | 01 篇 |
| libuv | 把 epoll / kqueue / IOCP 的差异抽象成一套异步接口，网络代码因此跨平台 | 01 篇 |
| 网络 I/O 与线程池 | 网络 I/O 走 OS 就绪通知、不占线程；`fs`、`dns.lookup`、`crypto`、`zlib` 才进池 | 01 篇 |
| 背压与 `drain` | `write()` 返回 `false` 就要停，等 `'drain'` 再继续，否则上下游速度差会把内存冲爆 | 01 篇 |
| HTTP 报文边界 | 头与体靠一个空行分界，体的结束靠 `Content-Length` 或 `chunked` | 02 篇 |
| `req` / `res` 是流 | 请求体要 `chunks[] + Buffer.concat` 累积；响应可多次 `write()` 再 `end()` | 02 篇 |
| `chunked` 与 `Content-Length` | 流式响应算不出总长度改用 chunked；能算时 `Content-Length` 必须是字节长度（`'你好'` 是 6 不是 2） | 02 篇 |
| Keep-Alive | 多个 HTTP 请求复用同一条 TCP 连接，省掉反复握手挥手的成本 | 02 篇 |
| 三个超时 | `headersTimeout` 必须大于 `keepAliveTimeout`，否则竞态丢请求、表现为 `ECONNRESET` | 02 篇 |
| TLS 三件事 | 加密、身份认证、数据完整性；生产上 TLS 常在网关终结 | 02 篇 |
| H1.1 队头阻塞与 H2 多路复用 | H1.1 同连接串行，浏览器只能多开约 6 条连接绕过；H2 单条 TCP 上多 Stream 的帧交错传输 | 02 篇 |
| HTTP/3 与 QUIC | 换到 UDP 之上做用户态可靠传输，让各 Stream 独立，避开 TCP 层队头阻塞 | 02 篇 |
| WebSocket 握手 | 一次 HTTP Upgrade：`Sec-WebSocket-Key` → `101` + `Sec-WebSocket-Accept`，之后脱离 HTTP 语义 | 03 篇 |
| 长连接心跳 | 假死连接的 Socket 还在却收不到数据，连续多次无 Pong 就判定失效并释放 | 03 篇 |
| SSE 的报文与续传 | 事件按 `event:` / `data:` / `id:` / `retry:` 分行、空行分隔；重连带 `Last-Event-ID` 从断点续推 | 03 篇 |
| SSE 与 WebSocket 选型 | 只需单向推送选 SSE，需要高频双向才选 WebSocket | 03 篇 |
| 31 步链路 | 前 29 步送请求、第 30 步是你的业务、最后 1 步送响应 | 04 篇 |
| 502 与 504 | 502 是网关没连上上游或拿到非法响应，504 是连上了但上游迟迟不回 | 04 篇 |

## 见 X 想 Y

现象是表相，先定位到层，再动手。

| 见到什么 | 该想到什么 | 一句话理由 |
| --- | --- | --- |
| `ECONNREFUSED` | 连接根本没建立，目标 IP:端口没有进程监听 | 报错发生在三次握手之前，先查服务起没起、端口绑没绑 |
| `ECONNRESET` | 连接建立过、中途被对端重置 | 大半是 Keep-Alive 两侧超时打架，先看谁先关的连接 |
| `ETIMEDOUT` | 包发出去了但始终没有回应 | 多是防火墙静默丢包或地址不可达，用 `telnet` / `nc` 探端口 |
| `socket hang up` | 响应还没回来，连接就被对方提前关了 | 查上游进程是否存活、有没有异常退出 |
| `EADDRINUSE` | 服务根本起不来 | 端口已被占用，多半是旧进程没退干净 |
| `Too many open files` | 文件描述符耗尽，多半是连接泄漏 | 每个连接占一个 fd，长连接服务要同时设 fd 上限与连接数上限 |
| `502 Bad Gateway` | 网关没连上上游，或拿到非法 / 不完整响应 | 查上游进程、端口监听、健康检查是否误摘流 |
| `504 Gateway Timeout` | 网关连上了，但上游在超时内没响应完 | 查慢查询与下游依赖，别用调大代理超时掩盖慢查询 |
| 证书校验失败 | 落在 TLS 握手那一段，不是业务代码 | 查有效期、证书链是否完整、域名是否匹配、系统时间对不对 |
| `431 Request Header Fields Too Large` | 请求头体积超限 | 典型是 Cookie / 鉴权头累积过多，和响应体的 chunked 是两条线 |
| 两次 `write` 只收到一次 `data` | TCP 是字节流，不保留消息边界 | 要自己定长度前缀或分隔符，HTTP 靠 `Content-Length` 自己定了边界 |
| `write()` 返回 `false` | 下游消费跟不上，内部缓冲到阈值了 | 停下等 `'drain'`，或直接用 `pipe` / `pipeline` 让流自己协调速率 |
| CPU 高但 QPS 低 | 事件循环被同步任务占住了 | 就绪的连接排不上队，去找大循环、`readFileSync`、超大 `JSON.stringify` |
| 客户端频繁重连长连接 | 心跳间隔没盖住中间设备的空闲超时 | NAT 与代理会静默回收空闲连接，心跳必须比它们更短 |

## 易错点汇总

下面这些是把各篇的误区合并去重后的版本，每条都对应一个线上真会踩的坑。

- **"异步 = 线程池"**：网络 I/O（`net` / `http` / `https`）走操作系统就绪通知，全程不占线程池。只有 `fs`、`dns.lookup`、`crypto`、`zlib` 这类没有同类异步机制的工作才会进池。
- **"TCP 保证你发几次我收几次"**：TCP 只保证字节可靠有序，不保留 `write` 的次数与边界，还可能出现半包。要消息边界就自己加长度前缀或分隔符，并把不够一条的字节留在缓冲区。
- **"`headersTimeout` 与 `keepAliveTimeout` 随便配"**：`headersTimeout` 必须大于 `keepAliveTimeout`。否则连接按 Keep-Alive 超时被关、状态还没和请求头超时逻辑对齐，后续请求被直接丢弃，客户端只看到 `ECONNRESET`。
- **"`Content-Length` 用字符长度"**：它取的是字节长度，`'你好'.length` 是 2 而 `Buffer.byteLength('你好')` 是 6。算错会让客户端截断或报错。
- **"流式响应也先算 `Content-Length`"**：流式的意义就是不等数据产完先发，总长度根本算不出来，应该改用 `Transfer-Encoding: chunked`。能算的只有一次性生成好的小响应。
- **"`Sec-WebSocket-Accept` 是加密"**：它是客户端 Key 拼固定串后 SHA-1 + Base64 的结果，不含任何加密语义，只用来证明对方懂握手、防缓存代理误判升级请求。
- **"WebSocket 能替代 TCP"**：分层是 `WebSocket → TCP → IP`，它是应用层协议，只是把消息格式、帧切分、心跳标准化了。绕开它直接用 `net`，这些规则全要自己造。
- **"实时通信就等于 WebSocket"**：只需服务端单向推送时，SSE 是纯 HTTP 长连接、浏览器自带重连、网关免配 `Upgrade`，比 WebSocket 轻得多；为了单向广播上双向电台是白付运维成本。
- **"SSE 推完就不管了"**：必须监听 `req.on('close')` 清理定时器与引用。否则客户端断开后推送逻辑还在跑，定时器、连接对象与 fd 都留着，形成泄漏。
- **"长连接建好就行"**：客户端断网、切网、NAT 超时后连接会"假死"，没有心跳就会一直背着死连接，慢慢吃光内存与 fd。
- **"单机广播能直接扩到多实例"**：多实例后连接散落在不同进程，A 实例发不到 B 实例持有的连接，广播必然漏发。需要共享订阅关系或消息总线。
- **"学底层是为了以后裸写 TCP"**：目的是拿到框架之下的坐标系——知道 `req` 是可读流、`res.end()` 触发的是一次 Socket 写、`ECONNRESET` 该去查连接生命周期。

## 与相邻模块的接口

上一模块给的是机制，本模块把机制落到具体链路上；本模块产出的坐标系，正是下一模块读框架源码的起点。

**进入本模块前必须已经掌握**

模块一「运行环境」给了四样东西，本模块正是它们的"真实来源"：

- **Event Loop**：模块一讲它分几个阶段，本模块说清 poll 阶段收的是内核的 I/O 就绪事件——"轮询"是问内核"谁就绪了"，不是"谁有数据了"。
- **libuv**：模块一讲它是异步 I/O 的调度层，本模块说清它底下屏蔽的是 epoll / kqueue / IOCP 的差异。
- **Buffer**：网络字节流进出的第一手形态就是 Buffer，HTTP 报文与 TCP 字节流落到 JS 里都是一段段 Buffer，而不是现成的字符串或对象。
- **Stream**：`net.Socket` 本身就是 Duplex Stream，所以 `req` 是可读流、`res` 是可写流——这个"流"来自 Socket，不是来自文件。

**本模块交给下一模块什么**

- 交给模块三「Web 框架基础」的是**「框架底部到底发生了什么」**：`node:http` 解析出的 `req` / `res` 是一对可读 / 可写流，路由匹配发生在 `'request'` 事件之后。
- 有了这个坐标，读 Express 的最小实现才能看懂"为什么只能这么写"：中间件之所以能用 `next()` 串起来，是因为它们叠加在同一对 `req` / `res` 上；`res.end()` 之所以能结束响应，是因为它最终触发一次 Socket 写。
- 同时要分清层次：`@Body()` 不会自动校验、异常要靠 `ExceptionFilter` 统一兜底，这些是框架层的问题，与网络层无关——排障时才不会串层。

## 代码与自测入口

- 配套代码在 `E:\working\blog\node\网络编程与实时通信\code\net-lab`：进目录先 `npm install`（只装 `ws` 一个包），再按 `npm run 01` ～ `npm run 08` 逐个跑。
- 跑的顺序就是这条线：裸 TCP → 粘包 → 自定义分包协议 → `node:http` → Keep-Alive → SSE → WebSocket；01+02 与 04s+04c 两组要开两个终端，其余脚本单终端自包含。
- `code/net-lab/README.md` 里逐脚本列了运行命令、要开几个终端与预期输出，跑之前先对一眼，避免把"粘包"当成 bug。
- 服务类脚本（01、04s、05）会占住 3000 / 4000 / 4002 端口，跑完记得 Ctrl+C 释放，否则下次启动会撞上 `EADDRINUSE`。
- 本模块面试题：[模块面试题](./面试题.md)（34 题，含一道 WebSocket 实时通知场景设计题）。
- 各篇末尾的 `## 参考` 里也有回到本页的入口。

## 参考

- [TCP 与 Socket 编程](./TCP%20与%20Socket%20编程.md)
- [HTTP 与 HTTPS 深入](./HTTP%20与%20HTTPS%20深入.md)
- [WebSocket 与 SSE 实时通信](./WebSocket%20与%20SSE%20实时通信.md)
- [一次请求完整经历了什么](./一次请求完整经历了什么.md)
- [模块面试题](./面试题.md)
- 上一模块：[模块一 · 运行环境 总结](../运行环境/总结.md)
- 下一模块：[模块三 · Web 框架基础 总结](../Express%20与%20Koa/总结.md)

读完这 5 篇，应该能答上三件事：TCP 是字节流、应用层怎么自己定消息边界；网络 I/O 不占 libuv 线程池，单线程凭什么扛住上万连接；服务端持续推送的场景为什么优先考虑 SSE。
