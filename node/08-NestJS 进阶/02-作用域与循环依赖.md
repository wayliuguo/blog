# 作用域与循环依赖

> 承上：[IOC 与依赖注入](../07-NestJS%20入门/02-IOC%20与依赖注入) —— Provider 的作用域与生命周期是 DI 容器的延伸，先理解容器才谈得清单例/请求级
> 启下：[登录注册实战](./03-登录注册实战) —— 用 bcrypt 加密密码并配合 `@nestjs/jwt`/Passport 实现一个能注册、登录、签发与校验 JWT 的认证接口

---

## Provider 作用域

Provider 的作用域决定了实例的生命周期。

```typescript
import { Injectable, Scope } from '@nestjs/common'

// DEFAULT（默认）：单例模式，整个应用共享一个实例
@Injectable()
export class SingletonService {}

// REQUEST：每个请求创建一个新实例
@Injectable({ scope: Scope.REQUEST })
export class RequestScopedService {
    constructor(@Inject(REQUEST) private request: Request) {}
}

// TRANSIENT：每次注入都创建一个新实例
@Injectable({ scope: Scope.TRANSIENT })
export class TransientService {}
```

### 作用域对比

| 作用域 | 说明 | 使用场景 |
|--------|------|---------|
| `DEFAULT`（单例） | 所有请求共享一个实例 | 大部分 Service |
| `REQUEST`（请求级） | 每个请求创建新实例 | 获取请求上下文 |
| `TRANSIENT`（瞬态） | 每次注入创建新实例 | 无状态工具类 |

### 请求作用域的使用

```typescript
@Injectable({ scope: Scope.REQUEST })
export class RequestLogger {
    constructor(@Inject(REQUEST) private request: Request) {
        console.log('新请求:', request.url)
    }

    log(message: string) {
        console.log(`[${this.request.url}] ${message}`)
    }
}
```

## 循环依赖

### 什么是循环依赖

```typescript
// user.service.ts
@Injectable()
export class UserService {
    constructor(private readonly orderService: OrderService) {}
}

// order.service.ts
@Injectable()
export class OrderService {
    constructor(private readonly userService: UserService) {}
}
```

`UserService` 依赖 `OrderService`，`OrderService` 又依赖 `UserService`，形成循环。

### 使用 forwardRef 解决

```typescript
import { forwardRef, Inject } from '@nestjs/common'

@Injectable()
export class UserService {
    constructor(
        @Inject(forwardRef(() => OrderService))
        private readonly orderService: OrderService
    ) {}
}

@Injectable()
export class OrderService {
    constructor(
        @Inject(forwardRef(() => UserService))
        private readonly userService: UserService
    ) {}
}
```

### 模块间循环依赖

```typescript
@Module({
    imports: [forwardRef(() => OrdersModule)],
    providers: [UserService],
    exports: [UserService]
})
export class UsersModule {}

@Module({
    imports: [forwardRef(() => UsersModule)],
    providers: [OrderService],
    exports: [OrderService]
})
export class OrdersModule {}
```

## 请求作用域的性能影响

使用 `Scope.REQUEST` 需要注意：

```typescript
// 如果 A 是单例，但注入了 B（请求作用域）
// 那么 A 实际上也会变成请求作用域
// 这会级联影响所有依赖链上的 Provider

@Injectable()  // 原本是单例
export class UserService {
    constructor(
        private readonly requestLogger: RequestLogger  // 请求作用域
    ) {}
}

// 实际上 UserService 也会变成请求作用域
// 每次请求都会创建新的 UserService 实例
```

**性能影响**：

- 请求作用域 = 每次请求创建新实例
- 创建实例需要时间，影响响应速度
- 频繁创建实例增加 GC 压力

**建议**：尽量使用单例，只在必要时使用请求作用域。

---

## 面试题

### Q1: NestJS 的 Provider 默认是什么作用域？REQUEST 作用域有什么影响？

默认是单例（DEFAULT），整个应用共享一个实例。REQUEST 作用域每个请求创建新实例，会影响性能（创建实例开销 + GC 压力），且会级联影响依赖它的单例 Provider。

### Q2: 循环依赖是什么？如何解决？

循环依赖指两个或多个模块/服务相互依赖。使用 `forwardRef()` 函数延迟解析依赖关系，告诉 NestJS 先创建占位，等对方准备好后再注入。

---

## 配套代码

本篇的可运行示例在仓库 `code/node/nestjs-mini`。

| 文件 | 演示什么 |
| --- | --- |
| `index.ts` | 容器构造与依赖解析过程 |

运行方式见 `nestjs-mini/README.md`。

---

## 参考

- 上一篇：[自定义装饰器](./01-自定义装饰器)
- 下一篇：[登录注册实战](./03-登录注册实战)