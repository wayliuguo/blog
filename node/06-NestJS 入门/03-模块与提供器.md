# 模块与提供器

---

## [初级] @Module 装饰器

`@Module` 是 NestJS 组织代码的基本单元，每个应用至少有一个根模块。

```typescript
import { Module } from '@nestjs/common'

@Module({
    imports: [],      // 导入其他模块
    controllers: [],  // 该模块下的控制器
    providers: [],    // 该模块下的服务/提供器
    exports: []       // 导出给其他模块使用的提供器
})
export class AppModule {}
```

## [初级] 模块化组织代码

```typescript
// users.module.ts
@Module({
    controllers: [UserController],
    providers: [UserService],
    exports: [UserService]  // 导出 UserService 以便其他模块使用
})
export class UsersModule {}

// app.module.ts
@Module({
    imports: [UsersModule],  // 导入用户模块
    controllers: [AppController],
    providers: [AppService]
})
export class AppModule {}
```

## [中级] 模块共享

通过 `exports` 导出 Provider，其他模块导入后即可使用：

```typescript
// 数据库模块：提供数据库连接
@Module({
    providers: [DatabaseService],
    exports: [DatabaseService]  // 导出 DatabaseService
})
export class DatabaseModule {}

// 用户模块：使用数据库服务
@Module({
    imports: [DatabaseModule],  // 导入数据库模块
    providers: [UserService],
})
export class UsersModule {}
```

## [中级] 全局模块

使用 `@Global()` 将模块标记为全局，它的 Provider 在所有模块中可用，无需导入：

```typescript
@Global()
@Module({
    providers: [ConfigService],
    exports: [ConfigService]
})
export class ConfigModule {}
```

> 注意：全局模块只用于真正需要全局共享的模块（如配置、日志），不要滥用。

## [中级] 动态模块

动态模块允许在导入时传入配置参数，广泛用于库模块：

```typescript
// 定义动态模块
@Module({})
export class DatabaseModule {
    static forRoot(options: DatabaseOptions): DynamicModule {
        return {
            module: DatabaseModule,
            providers: [
                {
                    provide: 'DATABASE_OPTIONS',
                    useValue: options
                },
                DatabaseService
            ],
            exports: [DatabaseService]
        }
    }
}

// 使用时传入配置
@Module({
    imports: [
        DatabaseModule.forRoot({
            host: 'localhost',
            port: 3306,
            database: 'myapp'
        })
    ]
})
export class AppModule {}
```

### forRoot vs forRootAsync

```typescript
// 同步配置
DatabaseModule.forRoot({ host: 'localhost', port: 3306 })

// 异步配置：从 ConfigService 读取配置
DatabaseModule.forRootAsync({
    imports: [ConfigModule],
    inject: [ConfigService],
    useFactory: (config: ConfigService) => ({
        host: config.get('DB_HOST'),
        port: config.get('DB_PORT')
    })
})
```

---

## 面试题

### Q1: 模块的 `exports` 和 `imports` 的作用？

`imports` 导入其他模块的功能，`exports` 导出当前模块的功能给其他模块使用。只有导出的 Provider 才能被导入该模块的其他模块使用。

### Q2: 动态模块的 `forRoot` 和 `forRootAsync` 有什么区别？

`forRoot` 是同步配置，适合配置值已知的场景；`forRootAsync` 是异步配置，适合需要从配置文件、环境变量或远程服务获取配置的场景。

---

## 参考

- 上一篇：[IOC 与依赖注入](./02-IOC%20与依赖注入)
- 下一篇：[控制器与路由](./04-控制器与路由)