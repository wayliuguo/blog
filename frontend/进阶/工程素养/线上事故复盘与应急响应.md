# 线上事故复盘与应急响应

事故处理里最贵的不是修 bug，而是**混乱**：不知道该谁来处理、不知道现在该止血还是该查根因、复盘写了但行动项三个月后还在原地。这一篇用 `practice-lab` 的 `incident.cjs` 把三件事做成可判定的规则：**分级**、**时间线拆解**、**行动项闭环**。

## 一、分级：先定级，再决定谁来

「线上有问题」这句话无法行动。定级要同时看**影响面**与**是否有资损/核心功能是否可用**：

> 摘自 `./code/practice-lab/incident.cjs`

```js
function grade(i) {
  // P0：核心功能不可用或资损；P1：核心功能降级或大范围受影响；P2：有一定影响且无绕过；P3：轻微且可绕过
  if (i.coreBroken || i.moneyLoss > 0) return 'P0'
  if (i.affectedUsers >= 10000 || i.coreDegraded) return 'P1'
  if (i.affectedUsers >= 500 || !i.workaround) return 'P2'
  return 'P3'
}
```

实测几条边界：

| 情况 | 级别 | 关键理由 |
| --- | --- | --- |
| 核心功能不可用，只有 50 人受影响 | **P0** | 核心不可用与人数无关 |
| 3 人受影响，但资损 12000 元 | **P0** | 有资损即 P0 |
| 5 万人受影响，有绕过方案 | **P1** | 大范围 |
| 300 人受影响，无绕过方案 | **P2** | 影响小但用户没办法自己解决 |
| 300 人受影响，有绕过方案 | **P3** | 可绕过 → 降级处理 |

**核心不可用 / 有资损 → P0，与影响人数无关**，这是最容易吵的一条。理由很简单：核心功能挂掉时，受影响人数只是「已经被观测到的」，真实规模随时间增长；而资损一旦发生就已经是事实。

定级必须**先于讨论**：级别决定谁被叫起来、多久响应、要不要升级——先讨论再定级，等于每次都从零开始协商。

## 二、时间线：MTTD 与 MTTR 是两个不同的问题

一次事故的总时长拆成两段：

> 摘自 `./code/practice-lab/incident.cjs`

```js
function timeline(ev) {
  const mttd = ev.detectedAt - ev.occurredAt // 多久被发现
  const mttr = ev.recoveredAt - ev.detectedAt // 发现后多久恢复
  return { mttd, mttr, total: mttd + mttr }
}
```

实测：01:00 发生、01:35 被**用户投诉**发现、01:50 回滚恢复 → **MTTD 35 分钟、MTTR 15 分钟**，合计 50 分钟，且瓶颈在「发现」而不是「恢复」。补上监控之后：MTTD 降到 5 分钟，MTTR 仍是 15 分钟。

这两段对应完全不同的改进：

| 段 | 慢的原因 | 改什么 |
| --- | --- | --- |
| MTTD（发现慢） | 没监控 / 告警阈值不合理 / 告警没人看 | 核心链路埋点、告警分级、值班响应 |
| MTTR（恢复慢） | 没有预案 / 回滚要重新发版 / 依赖别人 | 一键回滚开关、预案演练、权限前置 |

把两者混在一起说「我们响应太慢」，就会去改错误的地方——本例中如果去优化回滚流程，收益是 0，因为恢复本来就只花 15 分钟。

**响应时的顺序也要固定**：先止血（回滚 / 降级 / 限流），再查根因。在 P0 现场追根因是最常见的失误——用户还在受影响，而你在读日志。

## 二·一、一次完整 P0：时间花在哪一环

「我们响应太慢」这句话没有信息量，因为它没说慢在哪。把一次事故的时间线切成五段，就能排优先级。下面是一次有真实感的 P0：

> 摘自 `./code/practice-lab/incident.cjs`

```js
function breakdown(t) {
  const seg = {
    '发生 → 发现': t.detectedAt - t.occurredAt,
    '发现 → 有人接手': t.ackAt - t.detectedAt,
    '接手 → 定位': t.diagnosedAt - t.ackAt,
    '定位 → 决定回滚': t.decisionAt - t.diagnosedAt,
    '决定 → 恢复': t.recoveredAt - t.decisionAt
  }
  let total = 0
  for (const k of Object.keys(seg)) total += seg[k]
  return { seg, total }
}

// 每个改进只压它对应的那一段：改别的段是无效的——这正是要算清楚的东西
const FIXES = {
  '补监控告警（错误率超基线 2 倍即呼值班）': { '发生 → 发现': 3 },
  '告警直接呼值班，不等客服转达': { '发现 → 有人接手': 2 },
  '一键回滚开关，不用重新发版': { '决定 → 恢复': 2 }
}
```

时间线（当天 20:10 发布 v2.8.0，含下单页重构，灰度 50%）：

| 时刻 | 发生了什么 |
| --- | --- |
| 20:10 | 发布完成，错误率曲线开始抬升——**没有配告警，没人看到** |
| 20:41 | 客服收到第一条投诉（31 分钟后才被发现） |
| 20:55 | 值班同学被拉起 |
| 21:03 | 定位到下单页白屏 |
| 21:12 | 决定回滚 |
| 21:26 | 重新构建、发版，恢复完成 |

切段之后：

| 段 | 耗时 | 占比 |
| --- | --- | --- |
| 发生 → 发现 | 31 分钟 | 41% |
| 发现 → 有人接手 | 14 分钟 | 18% |
| 接手 → 定位 | 8 分钟 | 11% |
| 定位 → 决定回滚 | 9 分钟 | 12% |
| 决定 → 恢复 | 14 分钟 | 18% |
| **合计** | **76 分钟** | |

三个候选改进，各自只压它对应的那一段：

| 改进 | 改进后总时长 | 省下 |
| --- | --- | --- |
| 补监控告警（错误率超基线 2 倍即呼值班） | 48 分钟 | **28 分钟** |
| 告警直接呼值班，不等客服转达 | 64 分钟 | 12 分钟 |
| 一键回滚开关，不用重新发版 | 64 分钟 | 12 分钟 |
| 三项都做 | 24 分钟 | 52 分钟 |

这张表改变了两件事：

1. **排序**。最贵的是"发现"，不是"恢复"。如果凭直觉去优化回滚流程，收益是 12 分钟；补告警是 28 分钟，差一倍多。
2. **颗粒度**。"发现慢"还可以再拆——这次是「监控没配告警」（曲线在涨但没人看），和「告警没人接」是两件事，前者要补埋点阈值，后者要改值班机制。把 31 分钟整段叫"发现慢"，就会去改错的东西。

另外两处值得记的细节：**这次是灰度 50%**，所以影响面被限制在一半用户；**恢复靠重新发版**（14 分钟），而如果有不可变产物 + 入口切换，这一步是分钟级的。这两条正好对应[发布篇](../交付与质量/发布、灰度与回滚.md)里的不可变部署与一键回滚。

## 三、复盘：产出是关掉的行动项，不是文档

复盘的价值不在于那份文档写得多深刻，而在于**行动项有没有关掉**。每条行动项必须有 owner、截止时间、验收标准：

> 摘自 `./code/practice-lab/incident.cjs`

```js
  for (const a of actions) {
    if (!a.owner) {
      open.push(`${a.id}：缺 owner`)
      continue
    }
    if (!a.dueDay) {
      open.push(`${a.id}：缺截止时间`)
      continue
    }
    if (!a.acceptance) {
      open.push(`${a.id}：缺验收标准`)
      continue
    }
    if (!a.done && a.dueDay < nowDay) open.push(`${a.id}：已逾期 ${nowDay - a.dueDay} 天`)
  }
```

实测 5 条行动项：只有 1 条闭环（**20%**），另外 4 条分别有问题——1 条逾期 8 天、缺 owner、缺截止时间、缺验收标准各一条。

这三要素各自防的是一种典型失败：

- **缺 owner** → 所有人都以为是别人在做。
- **缺截止时间** → 永远排在下个迭代。
- **缺验收标准** → 做完了但说不清做没做完（"加了监控"——加了什么监控？）。

复盘的两个原则：

1. **对事不对人，但要具体到动作**。写"张三操作失误"没有价值；写"发布前缺少二次确认机制"才有。前者的行动项是"张三下次小心"（不可执行），后者是"发布流程加确认步骤"（可执行）。
2. **复盘要有期限**。48 小时内出初版、一周内定稿，超过这个时间，细节就开始丢失，复盘会退化成猜测。

## 四、日常演练：别让第一次实战发生在 P0

| 手段 | 频率 | 练什么 |
| --- | --- | --- |
| 回滚演练 | 每季度 | 开关真的能在分钟级生效吗 |
| 告警有效性检查 | 每月 | 阈值是不是早就该调了、有没有静默告警 |
| 混沌演练 | 每半年 | 依赖挂掉时降级链路通不通 |
| 值班手册走查 | 每季度 | 新人能不能照着手册独立处理 |

演练的意义在于：**降级开关这种东西，只有真的切过才知道它还在不在**。很多团队在 P0 现场第一次按那个开关，才发现它三个月前就被某个重构删掉了。

## 配套代码

| 文件 | 内容 | 对应小节 |
| --- | --- | --- |
| `./code/practice-lab/incident.cjs` | 事故分级 / MTTD·MTTR 时间线 / 行动项闭环 / 完整 P0 案例切段（4 场景） | 一、二、二·一、三 |
| `./code/practice-lab/proposal.cjs` | 方案必备项 / 取舍论证 / 风险矩阵（方案篇引用） | — |
| `./code/practice-lab/review.cjs` | CR 检查器 / 技术债打分 / 重构安全网（评审篇引用） | — |
| `./code/practice-lab/run.cjs` | 总入口：依次执行三探针 | 全篇 |

## 参考

- 本模块总结：[总结](./总结.md)
- 上一篇：[代码评审与技术债治理](./代码评审与技术债治理.md)
- 参考：[Google SRE：事故管理](https://sre.google/workbook/incident-response/) · [无指责复盘（blameless postmortem）](https://sre.google/sre-book/postmortem-culture/) · [混沌工程原则](https://principlesofchaos.org/)
