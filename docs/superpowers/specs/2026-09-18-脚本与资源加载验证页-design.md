# 01-HTML 基础：脚本与资源加载验证页 设计

> 日期：2026-09-18
> 关联：`frontend/基础/01-HTML 基础/code/`（配套代码）与 `01-HTML 语义化与结构.md`（正文）

## 1. 背景与目标

`frontend/基础/01-HTML 基础/code/site/` 现有两个示例：`semantic-blog/`（语义化 + 响应式图片）与 `register-form/`（表单）。正文第 10 节「脚本与资源加载：`src`/`href` 与 `defer`/`async`」目前**没有配套验证页**。

目标：删除 `semantic-blog/`（语义化/响应式图片知识点已在正文有充分代码示例，不再需要独立 demo），新增**「脚本与资源加载」验证页**，并同步更新对应文档的 `## 配套代码` 段与 `code/site/index.html` 目录页。

### 已确认决策（brainstorming 澄清结果）

- **删除 semantic-blog，新增脚本加载验证**：删 `code/site/semantic-blog/`；保留 `code/site/register-form/`（对应 02 篇，不动）。
- **验证范围（多选）**：执行顺序（defer 按文档顺序、async 下载完即执行不保序）、阻塞行为对比（同步 script 阻塞 vs defer/async 不阻塞）、首屏/白屏影响（同步脚本放 head 延迟首屏 vs defer 提前可见）。**不包含** src vs href 对比。
- **组织形式：多页独立**（方案 B）——`order.html` / `blocking.html` / `first-screen.html` 三个独立验证页，各自闭环。

## 2. 现状盘点（探索阶段实测）

- `code/site/` 现有：`index.html`（目录页，两个链接）、`semantic-blog/index.html`、`register-form/index.html`、`banner.svg`、`banner-wide.svg`。
- `server.js`：零依赖静态服务器，端口 5174，`ROOT = path.join(__dirname, 'site')`；MIME 表已含 `.js: 'text/javascript'`。无延迟/慢响应支持。
- 正文 `01-HTML 语义化与结构.md` 第 10 节（202-217 行）讲解 src/href、defer/async、`type="module"`；`## 配套代码` 段（265-275 行）指向 `semantic-blog/`，需改写。
- `02-表单与标签.md` 的 `## 配套代码` 段指向 `register-form/`，保留不变。

## 3. 新增文件（`code/site/script-loading/`）

| 文件 | 作用 |
| --- | --- |
| `order.html` | 执行顺序验证页。加载 `defer` 脚本 a/b + `async` 脚本 c/d（c 带 `?delay=800`）；页面内「执行日志」面板按时间戳记录每段脚本执行时刻与 `DOMContentLoaded` 时刻，直观显示 defer 按文档顺序、async 按下载完成顺序乱序。内联样式，自闭环 |
| `blocking.html` | 阻塞对比验证页。常驻一个跳动的计时器；「注入同步脚本」按钮以 `script.async = false` + `slow.js?delay=2000` 注入（计时器冻结约 2 秒），对照「注入 async 脚本」按钮（动态注入默认 async，不冻结） |
| `first-screen.html` | 首屏/白屏验证页。并排两个 iframe——`first-screen-sync.html`（head 同步 `slow.js?delay=2000`）vs `first-screen-defer.html`（同脚本 `defer`）；配「重新加载」按钮同时刷新两个 iframe，附观察要点说明 |
| `first-screen-sync.html` | 对照页：一段可见内容 + head 内同步 `slow.js?delay=2000` |
| `first-screen-defer.html` | 对照页：同内容 + head 内 `defer` 版 slow 脚本 |
| `order-a.js` / `order-b.js` | defer 脚本，执行时向日志面板追加「defer-a/defer-b」及时间戳 |
| `order-c.js` / `order-d.js` | async 脚本，同上（order-c 从页面以 `?delay=800` 引入） |
| `slow.js` | 慢脚本（配合 `?delay=` 使用），执行时打印时间戳 |

要点：

- 脚本通过 `window.__log(msg)`（在页面定义）向日志面板追加记录，时间戳用 `performance.now().toFixed(1)`。
- `order.html` 顶部给出一行预期结论文字，页面加载后日志面板即展示结果。
- `blocking.html` 的同步注入用 `script.async = false`（动态创建的 script 默认 `async = true`，需显式关掉才能演示同步阻塞）。

## 4. server.js 改动

`safePath` 解析 URL 时同时解析 `?delay=ms` 查询参数：`decodeURIComponent(url.split('?')[0])` 已剥离查询串，需在 `req.url` 上单独提取 delay 值；命中时 `setTimeout` 延迟发送响应（约 3 行）。其余逻辑不动。

## 5. 删除与目录页更新

- 删除 `code/site/semantic-blog/`；先 grep 确认 `banner.svg` / `banner-wide.svg` 无其他引用（正文/其他页面）后一并删除。
- `code/site/index.html` 目录页：移除 semantic-blog 链接，新增 script-loading 三个链接（order / blocking / first-screen），保留 register-form 链接。

## 6. 文档更新（`01-HTML 语义化与结构.md`）

`## 配套代码` 段（265-275 行）改写为：

```markdown
## 配套代码

本篇的可运行示例在仓库 `frontend/基础/01-HTML 基础/code/site/script-loading/`（对应正文第 10 节「脚本与资源加载」）。

| 文件 | 演示什么 | 对应小节 |
| --- | --- | --- |
| `script-loading/order.html` | `defer` 按文档顺序、`async` 按下载完成顺序乱序 | 第 10 节 |
| `script-loading/blocking.html` | 同步脚本阻塞页面（计时器冻结）vs `defer`/`async` 不阻塞 | 第 10 节 |
| `script-loading/first-screen.html` | 同步脚本放 head 的白屏 vs `defer` 提前首屏渲染 | 第 10 节 |

启动方式：在 `code` 目录执行 `node server.js`（即 `npm start`），打开 `http://localhost:5174/`。
```

正文第 10 节知识点内容不改；`## 小结` 不改。

## 7. 实施分工

1. 改 `server.js` 增加 `?delay=` 支持。
2. 新建 `code/site/script-loading/` 全部文件（9 个），三页内联样式自闭环。
3. 删除 `semantic-blog/` 与无引用的 banner svg；更新 `code/site/index.html` 目录页。
4. 改写正文 `## 配套代码` 段。
5. 验证：起 `node server.js` 实测三页行为（order 日志顺序 / blocking 冻结与不冻结 / first-screen 白屏差异）；`npm run prettier`；`npm run build`（vitepress 无 404）；精确 add 提交。

## 8. 验证标准（验收）

1. `code/site/script-loading/` 三页均可通过 `http://localhost:5174/` 访问，行为与第 3 节描述一致。
2. `semantic-blog/` 与无引用的 banner svg 已删除；`site/index.html` 无残留死链。
3. 正文 `## 配套代码` 表含「对应小节」列，路径与实际文件一致。
4. `npm run prettier` 与 `npm run build` 通过；提交仅含本任务相关文件。

## 9. 非目标

- 不新增 `code/README.md`（运行方式直接写入文章段落，沿用现状）。
- 不改正文第 10 节知识点内容与 `## 小结`。
- 不动 `register-form/` 及 `02-表单与标签.md`。
- 不演示 src vs href（未在验证范围内）。
